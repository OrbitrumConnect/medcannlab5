# DIARIO 31/05/2026 — Sessão UX/Visual densa + bug login Flávia cravado

**Estado de saída**: HEAD `cae44e9` (~16h BRT), 14 commits V1.9.534-544, 4 memórias Nível 1 novas, 1 migration, 1 fix dado-prod, locks 8 INTOCADOS.
**Pré-condição**: Pedro herdou estado do DIARIO_30 PARTE_2 (HEAD `59a1313`) com setup limpo + opção A (smoke UI V1.9.533) recomendada.
**Duração sessão**: ~10h corridas (manhã ~9h → tarde ~19h)
**Origem**: Pedro acordou + começou pelo prontuário (carry-over natural da V1.9.533 ontem) — não fez smoke UI mas fluiu direto pra novos gaps via uso real.

---

## 🌅 BLOCO A — Manhã: arquitetura limpa (~9h-12h)

### A.1 — V1.9.534 DROP trigger auto-TRIAGE drift + fix bug Card "Conversa com a Nôa"

**Gap empírico inicial Pedro**: screenshot prontuário Carolina mostrando 3 cards na aba Evolução com comportamento diferente:
- Card 1 "Avaliação clínica TRIAGE IN_PROGRESS `{}` vazio" — confuso
- Card 2 "Conversa com a Nôa" — **erro vermelho** "column patient_medical_records.title does not exist"
- Card 3 "Relatório AEC (IA Residente)" — completo e funcional

Pedro perguntou: *"qual a diferença pq um aparece dados outros nao?! duvida?!"*

**Investigação empírica via PAT + grep**:
1. Schema real `patient_medical_records`: id, patient_id, report_id, record_type, record_data, nft_token_id, created_at, updated_at — **ZERO coluna `title`**
2. Schema real `clinical_assessments`: tem `status` (in_progress/completed/reviewed) + `assessment_type` (TRIAGE/IMRE/null), MAS não tem `phase`
3. **56 órfãs `clinical_assessments` in_progress >7d** (não 1 isolada como pensei inicialmente)
4. EvolutionDetailModal.tsx:166+337-339 com bug: `SELECT title` em coluna inexistente

**Investigação MAIS profunda** revelou trigger pg `on_patient_created_triage` (AFTER INSERT users) auto-criando placeholder TRIAGE com `doctor_id = Ricardo` (fallback hardcoded) toda vez que paciente novo signup. Pedro perguntou: *"essas suas duvidas ja mapeamos no passado tem em diario memoria retrospectivas sobre doctor_id e clinical_assessments nao?!"* — e descreveu o comportamento esperado: *"todo paciente que cria conta loga nao tem medico ainda a nao ser que tenha add quem foi que indicou ou usando referal! ele so vincula apos aec quando gera a consulta e marca... e um comportamento normal n deve vincular ou criar fila para ninguem"*.

**Cravado**: trigger violava arquitetura validada 18/05+28/05 (`project_doctor_relation_card_design_18_05` + `feedback_share_overwrite_professional_id_28_05`). Vínculo médico-paciente SÓ via referral / appointment / AEC / share / admin_action — paciente decide. Auto-fallback Ricardo era **drift arquitetural sem dano clínico ativo**.

**Empírico crítico antes de DROP**:
- 43 pacientes únicos afetados / 56 rows
- 18/43 (42%) só tinham a órfã (abandonaram ANTES de abrir AEC — refuta hipótese "limbo causa abandono")
- 5/43 (12%) já tinham `clinical_report` assinado via fluxo real (FSM V1.9.299)
- 20/43 (47%) tinham `appointment` (vínculo posterior legítimo)
- 12/43 (28%) tinham `aec_assessment_state` (AEC real via tabela nova)
- InterruptedAECsCard V1.9.500 lê `aec_assessment_state` (não `clinical_assessments`) → fluxo real ignorava

**Migration `20260531120000_v1_9_534_drop_legacy_auto_triage.sql` aplicada**:
1. DROP TRIGGER `on_patient_created_triage` ON public.users
2. DROP FUNCTION `handle_new_patient_triage()`
3. UPDATE 45 rows existentes (`status='in_progress'` AND `assessment_type='TRIAGE'` AND `data='{}'` AND `updated_at < now() - interval '7 days'`) → `status='reviewed'` (preserva audit, não DELETE)
4. COMMENT documentando decisão

**Fix bug Card 2 mesmo commit** ([EvolutionDetailModal.tsx](src/components/EvolutionDetailModal.tsx)):
- Remove `title` do select linha 166
- Remove interface FullRecord.title
- Remove render `fullRecord.title` h3 linhas 337-339

**Dead code cleanup mesmo commit** ([NewPatientForm.tsx](src/pages/NewPatientForm.tsx)):
- 2 INSERTs `clinical_assessments` com `status='pending'` (linhas 386-396 + 519-540)
- CHECK constraint exigia `in_progress|completed|reviewed` → falha silenciosa
- Removidos com comentário V1.9.534

**Smoke pós-migration via PAT**:
```
trigger_exists:                0 ✅
function_exists:               0 ✅
reviewed_pos_migration:        45 ✅
triage_orfas_remanescentes:    1 (Flávia 30/05, <7d filtro — esperado)
type_check:                    verde ✅
push_4_refs:                   amigo + medcannlab5 × main + master ✅
```

Commit `45672ef` push 4 refs OK.

### A.2 — Material B externo amplificou gap "limbo paciente sem médico"

**Após V1.9.534 commitado**, Material B externo (provável Claude2/GPT) sugeriu memory anti-reversão: *"sem trigger, paciente novo abandona AEC porque 'ninguém vai ler'"*.

**Aplicação recursiva princípio cristalizado 29/05** [[feedback_auditoria_externa_cruzada_gpt_claude2_29_05]]:
- ✅ Memory anti-reversão = boa prática
- ❌ Hipótese "abandona por limbo" = especulativa
- ✅ Empírico HOJE: 18/43 abandonaram ANTES de abrir AEC (não chegaram em aec_state)

**Pedro pediu**: *"ue so checar oq ele falou se e real ou no"* — validei empíricamente:
- 4 pacientes type=paciente totais não-anonimizados (todos com appointment)
- 0 pacientes sem vínculo nenhum
- "Cenário limbo" não existe HOJE (Marco 2 zero)
- Apenas 1 caso real Mateus Chagas (aec_draft sem professional_id)

**Memory CALIBRADA cristalizada**: `project_v1_9_534_drop_trigger_auto_triage_modo_2_parqueado_31_05.md` — protege contra reversão SEM amplificar especulação.

### A.3 — V1.9.535 agrupa "Conversa com a Nôa" por dia + timeline modal

**Gap empírico Pedro via uso real**: ainda dentro do Card "Conversa com a Nôa" do screenshot inicial, perguntou *"e a mesma conversa? nao poderia estar junto a mesma ? precisa ser separado duvida?"*

**Empírico via PAT**:
- Casos extremos: Carolina Campello 27/04 = **236 turnos** chat em 23h (236 cards idênticos no prontuário)
- Joao Eduardo Vidal 27/01 = 201 turnos
- Pedro Paciente 23/04 = 188 turnos
- Maria das Dores 15/05 = 6 turnos em 5min (caso do screenshot original)

**Causa**: cada turno (1 user_message + 1 ai_response) é 1 row em `patient_medical_records` → frontend renderizava 1 card por row.

**Fix frontend-only zero migração**:
1. PatientsManagement.tsx: limit `.limit(20)` → `.limit(500)` (cobre Carolina 236 + outros extremos)
2. Separa `chat_interaction` (agrupa por dia) de outros (mantém 1:1)
3. Agrupa por `DATE(created_at)` BRT em `chatByDay: Map<dayKey, records[]>`
4. Cria 1 Evolution sintético por dia com `chatGroupIds[]` + `chatGroupCount` + `chatGroupFirstTime` + `chatGroupLastTime`
5. Card resumo: `"Conversa com a Nôa · N mensagens (HH:MM - HH:MM)"`

**EvolutionDetailModal.tsx**:
1. Nova interface `ChatTurn`
2. State `chatTurns: ChatTurn[] | null`
3. Quando `evolution.chatGroupIds.length > 0`: busca batch via `.in('id', chatGroupIds).order('created_at', { ascending: true })`
4. Renderiza timeline elite: 1 card por turno, time + Paciente (azul) + Nôa (emerald)
5. Fallback `fullRecord` antigo mantido pra rows sem `chatGroupIds`

Resultado UX:
- ANTES: Carolina 27/04 = 236 cards idênticos
- DEPOIS: Carolina 27/04 = 1 card "Conversa com a Nôa · 236 mensagens (00:01 - 23:29)" + modal timeline expandida

Commit `93e718b` push 4 refs OK. Memory cristalizada: `project_v1_9_535_agrupa_chat_interaction_por_dia_prontuario_31_05.md`.

---

## 🌤️ BLOCO B — Início tarde: Equipe Clínica + Material B (~12h-14h)

### B.1 — Pedro pediu avaliação aba Equipe Clínica com proposta externa 7 blocos

Pedro trouxe proposta Material B/C externo extensa: 7 blocos pra "Centro de Comando da Equipe" (Radar Operacional / Heatmap / Fallback Engine / Saúde de Verdade / Governança / Performance / Risco).

**Validação empírica via PAT + grep**:
- `TeamManagement.tsx` 1205 linhas (V1.9.186-C/D/E + V1.9.274 + V1.9.430) — **JÁ É** "Centro de Comando" com header literal
- 80% das funcionalidades propostas JÁ EXISTEM (teamHealthScore com fórmula, Mapa de Carga, ReferralsManager consent-first, Quick Actions WhatsApp+Chat, Top Performer, filtros+paginação, convites pendentes, glow status)
- Empírico DB: Eduardo 3d offline (não 25d como proposta inventou), 4 pacientes total (não 3 aguardando), 1 médico ativo na equipe Pedro (Ricardo 6d)

**Score honesto proposta externa**:
- B1 Tooltip Health Score: ✅ válido (fórmula só no código)
- B2 Heatmap reskin: ❌ Babylon (Mapa de Carga existe)
- B3 Match score 87% automático: ❌ Babylon (viola V1.9.274 consent-first)
- B4 "Há X dias" formato humano: ⚪ JÁ existia (formatMinutes linhas 279+418)
- B5 Eduardo 25d offline: ❌ Babylon (real=3d)
- B6 Score operacional 91: ❌ Babylon (autocontradito — "não vaidade" + propõe 91)
- B7 Bloco Risco: ✅ válido SE Risk Cockpit é admin-only

**Validação B7 admin-only**: grep `RiskCockpit` retornou 5 matches, todos em `AdminDashboard.tsx:7+85` case `'risco'`. Médico não-admin (Dayana futura) **NÃO** vê → B7 não é duplicação.

### B.2 — Meta-análise EXTERNA da minha análise (Material B/C #2)

Pedro trouxe SEGUNDA análise externa avaliando MINHA avaliação. 3 calibrações honestas aceitas:
1. "30% real + 70% Babylon" matematicamente impreciso → real 43%/57%
2. B7 precisa validação empírica Risk Cockpit admin-only (apliquei imediato)
3. Custo 45min subestima ciclo completo (60-75min real)

**Frase âncora cristalizada**: *"6º Material B/C externo da sessão hoje. Padrão `feedback_freeze_absorcao_material_b_pos_6h_sessao_29_05` aplicável."*

### B.3 — V1.9.536 tooltip Saúde Equipe + bloco alertas inatividade

**B1 fix** ([TeamManagement.tsx:125-153](src/components/TeamManagement.tsx#L125)):
- Tooltip multi-linha explicando fórmula: `acceptScore*0.4 + onlineRatio*30 + latencyScore*30`
- Limiar visual: 85+ Excelente / 70+ Bom / 50+ Atenção / <50 Crítico
- Ícone Info inline + cursor-help

**B7 fix** (novo `useMemo` + bloco render):
- `inactivityAlerts` agrega membros aceitos com `minutes_since_seen >= 7*1440`
- Limiares: >=30d critical (vermelho) / 7-30d warning (amber)
- Renderiza SILENCIOSAMENTE vazio quando equipe ativa
- Reusa `minutes_since_seen` já presente (zero query/migration/RPC)

**Smoke empírico**: 0 alertas hoje (Eduardo 3d / Ricardo-alt 6d abaixo limiar 7d) → bloco oculto = comportamento correto.

Commit `e526dcc` push 4 refs OK. Memory: `project_v1_9_536_team_health_tooltip_e_bloco_alertas_inatividade_31_05.md`.

### B.4 — Pedro flagrou empíricamente "muita escrita pouca usabilidade"

Após V1.9.536, Pedro disse direto: *"vejo muita escrita pouca usabilidade"*. Diferenciei: empírico Pedro ≠ Material B externo.

**4 ofensores cravados no screenshot**:
1. Banner amarelo "agenda cheia" — 2 parágrafos educacionais
2. Subtítulo header "Centro de Comando" — frase comprida
3. ReferralsManager parágrafo educacional — 3 frases inline
4. ReferralsManager colunas vazias — 200px pra dizer "Nenhuma sugestão" + "Nenhum direcionamento"

**V1.9.537 fix**:
1. Banner → 1 linha + tooltip detalhe (compactou)
2. Subtítulo → "Sua rede · presença em tempo real"
3. Parágrafo → tooltip do botão "Sugerir Direcionamento"
4. Colapsar quando totalmente vazio (flag `totallyEmpty`)

Commit `5523d36` push 4 refs OK.

---

## 🌅 BLOCO C — Início tarde: visual transversal app (~14h-15h)

### C.1 — Pedro flagrou background não-padrão do app

*"o back ground nao esta padrao app degrade correto avaliar"* sobre Terminal Integrado.

**Empírico via grep**: 28+ ocorrências `bg-[#0f172a]` em 9 arquivos — quebra padrão `backgroundGradient` do designSystem (`#0f172a → #1e293b → #1e3a3a` com toque teal escuro).

**V1.9.538 fix DRY**:
1. Criada utility CSS `.app-bg-gradient` em [src/index.css](src/index.css) (1 ponto canonical)
2. 28 substituições em 9 arquivos:
   - IntegratedWorkstation.tsx (14 ocorrências, todas abas Terminal)
   - ResearchWorkstation.tsx (8 ocorrências, todas abas Pesquisa)
   - ProfessionalMyDashboard.tsx (4 ocorrências)
   - ProfessionalDashboard.tsx (2 ocorrências)
   - AdminCasosSimilares.tsx + ExternalLiterature.tsx + PatientDashboard.tsx + RenalFunctionModule.tsx (1 cada)
3. NÃO mexido (legítimos): AdminDashboard cards transparentes (/50 /40) + Courses modal max-w-4xl

**Mesmo commit V1.9.538 fixou aba Agenda do prontuário** (Pedro flagrou *"tá fraco pode ficar top pro"*):
- KPIs reformulados com ícones distintivos + cores semânticas (Hoje cyan/Semana indigo/Confirmados emerald/Pendentes amber) + bordas coloridas + hover scale 1.02
- Botão "Novo Agendamento" duplicado REMOVIDO (estava header + Ações Rápidas)
- Empty state Agenda de Hoje promovido (card centralizado com ícone + hint)

Commit `717e0ab` push 4 refs OK.

### C.2 — Pedro flagrou mais ofensores (botões verdes/azuis sólidos + "Anexar arquivo")

4 gaps visuais consecutivos. **V1.9.539 batch fix**:

1. **Library.tsx**: removida declaração LOCAL `backgroundGradient` (`#0A192F → #1a365d → #2d5a3d` — cores diferentes do app) + surfaceStyle/secondarySurfaceStyle/accentGradient/secondaryGradient/goldenGradient redeclarados localmente. Agora usa imports designSystem.

2. **PatientsManagement.tsx**: 3 botões `bg-green-600`/`bg-blue-600` sólidos → padrão **translucent emerald**:
   ```
   bg-emerald-500/15 hover:bg-emerald-500/25
   border border-emerald-500/30
   text-emerald-300 hover:text-emerald-200
   hover:scale-[1.02]
   ```

3. **ProfessionalPatientFiles.tsx**: "Anexar arquivo" `bg-gradient-to-r from-purple-500 to-pink-500` → translucent emerald (roxo/rosa destoava identidade verde MedCannLab)

4. **IntegrativePrescriptions.tsx**: "Nova Prescrição CFM" `bg-emerald-600/90` sólido → translucent emerald + "Prescrever este protocolo" idem + **REMOVIDOS 2 placeholders fake "Mais protocolos em breve"** (poluía visual quando <3 templates)

**Padrão CTA app CRISTALIZADO neste commit** (vai pra memory `project_v1_9_537_541_padrao_visual_translucent_31_05`).

Commit `bccfed4` push 4 refs OK.

---

## 🌇 BLOCO D — Tarde 1: Evolução paginada + Workstation parity (~15h-16h)

### D.1 — Pedro flagrou aba Evolução render infinito

*"aqui embaixo veja precisa de paginacao 10 por vez e dropdown fica mais top elit cada sessao?! proficional escolhe a que quer abre etc?!"*

Empírico via screenshot Carolina: 39 Relatórios AEC + 20 Conversas + 1 Evolução médico = render vertical infinito (~60+ cards).

**V1.9.540 fix**:
1. Estados novos: `collapsedEvolutionGroups: Set<EvolutionKind>` (default `chat-ia` collapsed) + `evolutionGroupPages: Record<EvolutionKind, number>`
2. Reset paginação ao trocar paciente (loadPatientEvolutions reseta)
3. Header de cada grupo virou BUTTON colapsável (ChevronDown rotaciona)
4. Quando expandido E `items.length > 10`: paginação com Footer "Anterior / page/total / Próxima"
5. Defaults inteligentes: doctor-evolution/aec-report/assessment-other EXPANDED, chat-ia COLLAPSED (ruidoso por design)

Commit `e53eea9` push 4 refs OK.

### D.2 — Pedro: *"muito bom elevando o nivel de usabilidade top!"*

Pedro reconheceu evolução. Eu apresentei progressão observada (arquitetura → UX detalhado → visual transversal → escalabilidade). Padrão CTA cristalizado neste ciclo. Pedi se queria pausar ou continuar.

### D.3 — V1.9.541 — paridade visual Prescrição prontuário ↔ Workstation

Pedro comparou 2 screenshots (Workstation cards grandes coloridos vs Prontuário cards apagados com cadeado tímido): *"a do prontuario ta meio apagada nao acha?"*

**V1.9.541 fix** ([IntegrativePrescriptions.tsx](src/components/IntegrativePrescriptions.tsx)):
- Cards "Nova Receita" passaram de `px-3 py-2 text-xs` minúsculos → `p-3` com badge ícone `w-10 h-10 rounded-lg bg-[cor]-500/20 border` + ícone `w-5 h-5` (idêntico padrão Workstation)
- Tipografia: `text-xs` → `text-sm font-bold` (título) / `text-[10px]` → `text-[11px] text-slate-400` (subtítulo)
- Hover scale-105 (igual workstation)
- Cores semânticas mantidas: Simples emerald / Branca slate / Azul blue / Amarela amber

Commit `e125f56` push 4 refs OK.

---

## 🌃 BLOCO E — Tarde 2: bug aba Prescrição (~16h-17h)

### E.1 — Pedro: *"qndo clico em prescicao no prontuario tenta mudar e volta pro antigo pq sera? bug meu?"*

Testou em multi-navegador + Vercel produção + localhost. Reproduzia sempre.

**V1.9.542 fix BUG REAL** ([PatientsManagement.tsx:682-700](src/pages/PatientsManagement.tsx#L682)):
- useEffect tinha `[selectedPatient, searchParams]` como deps
- `searchParams` do react-router cria nova referência a cada render
- Click em aba → `setActiveTab('prescription')` → re-render → searchParams nova → useEffect dispara → URL não tem `?tab=` → `!tabParam=true` → `setActiveTab('overview')` RESETA

Fix: deps trocada de `[selectedPatient, searchParams]` (objeto+referência instável) → `[selectedPatient?.id]` (string estável). Click em aba não muda patient.id, então useEffect não dispara mais.

Commit `7f88f1a` push 4 refs OK.

### E.2 — Pedro: *"continua assim pq?"* — V1.9.543 debug

Bug PERSISTIA. Adicionei wrapper debug `setActiveTab` com console.warn + stack trace.

Pedro retornou com 2 screenshots: aba Prescrição mostrando 4 cards grandes vazios ("TOTAL —" / "ATIVAS —" / "DRAFT —" / "CONCLUÍDAS —") logo após clicar, depois mudando pra pill compacta "Sem prescrições pra este paciente ainda".

### E.3 — V1.9.543-B — não era bug de tab, era flash loading state

**Diagnóstico cravado pelos 2 screenshots**:
- LOADING: `prescriptionsLoading=true` → `!isEmpty` → renderiza 4 cards grandes com "—"
- LOADED + zero data: `isEmpty=true` → colapsa pra pill (V1.9.501 intencional)

Pedro interpretou loading→loaded como "tela voltando pro antigo". Na verdade era transição visual desnecessária.

**V1.9.543-B fix**: loading state agora usa MESMA pill compacta do empty state, só muda texto pra "Carregando prescrições..." com Loader2 spinner. **Reverteu também o debug V1.9.543** (não era bug de tab).

Commit `1d2d15e` push 4 refs OK.

---

## 🌃 BLOCO F — Final tarde: padronização 4K + bug login Flávia (~17h-19h)

### F.1 — V1.9.544 — padronização max-w-screen-2xl 4K/QHD

Pedro: *"o app ainda sinto que o conteudo total em algumas abas estao maiores outros menos leve inconsistencia... estou em uma tela de 41 polegadas...avaliar"*

**Empírico via grep max-w**:
- 15+ `max-w-7xl` (1280px)
- 7 `max-w-6xl` (1152px)
- 5 `max-w-5xl` (1024px)
- 4 `max-w-[1800px]`
- ProfessionalMyDashboard usava `container mx-auto` (trava xl=1280)

Em 41" 4K (3840px): max-w 1280 = ~2560px margem lateral vazia.

**Opção A escolhida** (padronização cirúrgica zero-risco):
- ProfessionalMyDashboard.tsx: `container mx-auto` → `max-w-screen-2xl` (1536px)
- TeamManagement.tsx: `max-w-7xl` → `max-w-screen-2xl`
- PatientDashboard.tsx: `max-w-7xl` → `max-w-screen-2xl`
- AlunoDashboard.tsx: `max-w-7xl` → `max-w-screen-2xl`

NÃO mexido: modais (max-w-4xl/2xl), AdminCasosSimilares/Library (max-w-[1800px] já elite), PatientsManagement (max-w-full intencional).

Comportamento esperado em 4K: +20% largura útil (1280→1536) + consistência visual entre 4 telas principais. Laptops <1536px: mx-auto usa 100% disponível (zero quebra).

Commit `cae44e9` push 4 refs OK.

### F.2 — Pedro pediu reiniciar localhost (cache bug)

`npx kill-port 3000 && npm run dev` → Vite v7.3.1 ready em 1143ms (Forced re-optimization of dependencies). Sistema voltou OK.

### F.3 — Avaliação geral por áreas (Pedro pediu visão holística)

Apresentei tabela 🟢/🔵/🟡/🟠/🔴 por área. Pedro pediu validação MAIS profunda.

**Validação empírica via PAT revelou ERROS minhas**:
- "AECs completas: 7+" → REAL: **4**
- "Reports assinados: 102" → REAL: **43 total / 25 em 30d** (102 era nº histórico desatualizado memória 27/04)
- "cfm_prescriptions: 32" → REAL: **52**
- "Edges ACTIVE: 14" → REAL: **15** (esqueci create-patient-auth V1.9.533)
- "Gamification DESABILITADA por flag" → REAL: **45 rows ATIVAS** (memória 24/04 desatualizada)

**Novo insight crítico**:
- Cron `monthly-closing-medcannlab` vai executar 01/jun ~3h BRT pela 1ª vez
- Vai processar 45 rows gamification_points existentes
- **Risco**: pode popular `ranking_history` indevidamente
- Memória 24/04 dizia "feature desabilitada" — empírico refuta

**Princípio meta cristalizado**: **memórias estáticas envelhecem** — preciso validar via PAT antes de afirmar números em avaliações futuras.

### F.4 — Pedro: *"chunk-...js?v=...:21609... Erro no login: Database error querying schema..."*

Console error CRÍTICO. Pedro tentou logar com a Flávia usando a senha que EU disse pra ele ontem (workaround V1.9.533 — INSERT SQL direto via PAT).

**Diagnóstico empírico**:
1. Reproduzido erro via API direta: `500 unexpected_failure` + `error_id: 019e7e9a-...`
2. Comparei Flávia (broken) vs Maria Pitoco (signup UI normal, OK):

| Campo | Flávia (SQL direto) | Maria Pitoco (signup UI) |
|---|---|---|
| `confirmation_token` | **NULL** | `""` |
| `recovery_token` | **NULL** | `""` |
| `email_change_token_new` | **NULL** | `""` |

**Causa**: Supabase Auth Service quebra com tokens NULL — espera string vazia `""`. Bug conhecido GitHub Supabase mas mascarado por mensagem genérica.

**Fix via PAT**:
```sql
UPDATE auth.users
SET confirmation_token = COALESCE(confirmation_token, ''),
    recovery_token = COALESCE(recovery_token, ''),
    email_change_token_new = COALESCE(email_change_token_new, ''),
    email_change_token_current = COALESCE(email_change_token_current, ''),
    phone_change = COALESCE(phone_change, ''),
    phone_change_token = COALESCE(phone_change_token, ''),
    email_change = COALESCE(email_change, ''),
    reauthentication_token = COALESCE(reauthentication_token, '')
WHERE id = '18ece941-2d7f-4e38-a649-6de4f2d366e7'
```

**Smoke pós-fix**: login retornou `access_token` JWT válido imediatamente. Empírico via API.

**Audit preventivo**: 0 outros users com tokens NULL. Apenas Flávia tinha. Bug isolado do meu workaround manual.

**Pedro confirmou empíricamente**: *"perfeito consegui logar top !"*

**Memory cristalizada**: `feedback_nunca_criar_auth_users_sql_direto_tokens_null_31_05.md`. Princípio META: **NUNCA INSERT SQL direto auth.users** — SEMPRE Edge `create-patient-auth` V1.9.533 (API nativa `auth.admin.createUser`) OR signup UI.

### F.5 — Avaliação fluxo financeiro/referral (Pedro perguntou)

Pedro descreveu fluxo esperado: *"quais quer usuarios add por proficional link referal ao logar tem o concentimento depois vao para aba financeira aonde tem o pagamento do plano! ficam la ate pagar ou ate serem liberados por adm... unica coisa que nao temos e stripe ainda mais ta configurado assim ne?"*

**Validação empírica**:
- ✅ Link referral existe (InvitePatient + QuickReferralModal + invited_by + referral_code) — 7 users com invited_by
- ✅ Consentimento (`consent_accepted_at` em 33 users)
- ✅ Aba financeira UI completa (PatientFinancialDashboard + PaymentCheckout + SubscriptionPlans + WalletTab)
- ✅ 3 planos cadastrados ATIVOS: Med Cann 150/250/350
- ⚠️ user_subscriptions: **0 ativas** (zero pagantes)
- ❌ **Stripe NÃO conectado** (confirmaste) — PaymentCheckout MOCK (PIX QR mock + setTimeout 2s) + WalletTab linha 269 menciona "Stripe Connect quando integração ativa"
- ❌ **Gate hard de bloqueio NÃO existe** (grep `hasActivePlan|requiresPayment|PaymentGate` = 0 matches)
- ❌ **UI/RPC admin "liberar paciente" NÃO existe** (grep `admin_grant|grant_user_access` = 0 matches)

**Distribuição empírica payment_status atual** (51 users total):
| Status | Qty | Significado |
|---|---|---|
| `paid` | 40 | Marcados como pagos (manualmente, todos teste) |
| `exempt` | 7 | Liberados por admin (manual via DB) |
| `pending` | 4 | Aguardando pagar (nunca cobrados real) |

**Conclusão honesta**: sistema financeiro está em estado "DEMO/MOCK" — não bloqueia ninguém, não cobra ninguém. Funcional pra demonstrar fluxo, **mas zero enforcement real**. Faz sentido pré-PMF (zero pagantes externos + CNPJ João pendente).

---

## 📝 BLOCO G — Falas/decisões importantes do dia

### Empíricas Pedro
- *"vejo muita escrita pouca usabilidade"* — disparou V1.9.537 compactação
- *"o back ground nao esta padrao app degrade correto avaliar"* — disparou V1.9.538-539 visual transversal
- *"a do prontuario ta meio apagada nao acha?"* — disparou V1.9.541 paridade Workstation
- *"precisa de paginacao 10 por vez e dropdown fica mais top elit cada sessao"* — disparou V1.9.540
- *"clico em prescicao no prontuario tenta mudar e volta pro antigo pq sera? bug meu?"* — disparou V1.9.542 (não era teu bug)
- *"continua assim pq?"* — disparou V1.9.543 debug + V1.9.543-B fix loading state
- *"estou em uma tela de 41 polegadas...avaliar"* — disparou V1.9.544 max-w-screen-2xl
- *"sim mais profundamente da pra validar isso tudo que vc falou?"* — disparou validação empírica que revelou 5 erros nas minhas afirmações
- *"perfeito consegui logar top ! agora tudo coerente sem erros ?"* — confirmação bug Flávia resolvido

### Princípio meta cristalizado por Pedro
- *"essas suas duvidas ja mapeamos no passado tem em diario memoria retrospectivas sobre doctor_id e clinical_assessments nao?!"* — antes de eu propor mudança, validar memórias existentes (aplicado em V1.9.534 trigger drift)
- *"todo paciente que cria conta loga nao tem medico ainda a nao ser que tenha add quem foi que indicou ou usando referal... comportamento normal n deve vincular ou criar fila para ninguem"* — princípio consent-first cristalizado, fundamentou drop trigger V1.9.534

### Recalibrações empíricas honestas do dia
1. Eu disse "40% lixo histórico dev" sobre 56 órfãs TRIAGE → empírico revelou **80% pacientes REAIS abandonados** (recuei)
2. Eu sugeri trocar `type='professional'` → `paciente` em 8 contas → Pedro flagrou *"eles sao pro pq mudar para pacientes?"* → recuei (cadastros incompletos legítimos)
3. Eu disse "AECs completas 7+" → empírico **4**
4. Eu disse "Reports 102" → empírico **43 total / 25 em 30d**
5. Eu disse "Gamification desabilitada" → empírico **45 rows ATIVAS** (memória 24/04 stale)
6. Eu disse "Edges 14" → empírico **15**

### Material B/C externos absorvidos hoje (contagem cumulativa: 8)
1. Proposta TeamManagement 7 blocos (filtrei 3 válidos + 4 Babylon)
2. Meta-análise da minha análise (aceitei 3 calibrações)
3. Sugestões loading state Prescrição (não chegou — Pedro foi direto)
4. Outras ao longo do dia (avaliação geral, fluxo financeiro)

**Padrão `feedback_freeze_absorcao_material_b_pos_6h_sessao_29_05`** aplicado recursivamente: aceitei calibração honesta, recusei implementação ampliada (Modo 2 UI parqueado).

---

## 📊 BLOCO H — Estado final empírico (validado via PAT)

| Métrica | Valor |
|---|---|
| **HEAD** | `cae44e9` |
| Commits hoje 31/05 | **14** (V1.9.534+535+536+537+538+539+540+541+542+543+543-B+544 = 12 versões + 2 patches) |
| Total commits projeto | 1038+ |
| Memórias persistentes | 308 |
| Memórias Nível 1 cristalizadas hoje | 4 (534+535+536 + nunca_INSERT_SQL_auth_users) |
| Migrations | 1 (V1.9.534) |
| Utility CSS nova | 1 (.app-bg-gradient) |
| Edges ACTIVE | 15 (14 verify_jwt=true + 1 false sign-pdf-icp lock) |
| Cobertura defesa em camadas | 93.3% (14/15) |
| RLS coverage | 100% (145/145 tabelas) |
| Cron jobs ativos | 4 (3 OK rodando, 1 aguarda 01/jun) |
| Locks regulatórios INTOCADOS | 8 ✅ |
| Bombas latentes desarmadas | 2 (trigger drift V1.9.534 + tokens NULL Flávia) |
| Bugs cravados + resolvidos | 3 (Card Conversa Nôa / aba Prescrição loading flash / login Flávia) |
| Material B externos filtrados | 8 |

---

## 🚧 BLOCO I — Pendências polir/calibrar SEM regressão (carry-over)

### 🟢 Cirúrgicas técnicas (~10-30min cada)
- **Smoke real V1.9.533 via UI**: cadastrar paciente teste pela UI confirmando modal senha + login funciona
- **V1.9.534 (próximo número livre)**: força troca senha primeiro acesso (lê `must_change_password` user_metadata)
- **V1.9.535 (próximo livre)**: toggle UI no NewPatientForm "Enviar email automático?"
- **V1.9.536 (próximo livre)**: migration backfill 5 órfãos antigos via Edge create-patient-auth

### 🟡 Decisões humanas pendentes (Pedro lado)
- **WhatsApp Ricardo**: passar credenciais Flávia (`flaviatheodoromotta@gmail.com` / `X9kMpQ!8Lv@n3MedCann2026#Flv`) + 4 AECs INTERRUPTED + Illa Proença + V1.9.541 cards prescrição
- **WhatsApp João Vidal**: status CNPJ + reunião sócios v2.1 (joão 23d offline)
- **WhatsApp Eduardo Faveret**: status sócio operacional (3d offline)
- **WhatsApp Claudio externo**: resposta calibrada às auditorias do dia (8 Material B)
- **Promoção acordo v2.1**: NÃO oficial até reunião 4 sócios + Paulo contador + advogado

### ⚠️ AGENDA AUTOMÁTICA 01/jun (amanhã)
- **~03h BRT**: cron `monthly-closing-medcannlab` 1ª execução (vai processar 45 gamification_points)
- **~15h BRT**: decisão hard-delete batch 5 Edges em observação 48h
- **Sondar 01/jun cedo**: ranking_history populado? gamification comportamento correto?

### ❄️ Parqueados conscientes (sem trigger empírico)
- **PaymentGate enforcement** (depende Marco 2)
- **Stripe Connect setup** (depende CNPJ João — Marco 1)
- **Edge create-checkout-stripe** + webhook (depende Stripe Connect)
- **UI/RPC admin_grant_access** (depende cobrança real começar)
- **Modo 2 DoctorRelationCard** (parqueado até Marco 2 + reclamação real Ricardo)
- **WiseCare migração homolog → prod** (P0 listado sem trigger)
- **4 triggers auth.users race condition** (parqueado V1.9.520)
- **SGQ ANVISA petição** (20 drafts em docs/sgq/ pré-Marco 2)
- **BYO-LLM** (parqueado pós-Marco 1)
- **Refator tradevision-core 7765L** (parqueado pré-PMF aceitável)
- **Tier 3 chat_interaction Matrix Z2** (Marco 2)
- **V1.9.451 function calling Edge** (gap empírico Ricardo 24/05)
- **V1.9.456 QR Code visual embedded PDF** (depende smoke ITI completo)

### 🐛 Bugs latentes documentados sem impacto clínico
- **gamification ATIVA sem governance**: 45 rows real, memória 24/04 dizia desabilitada — refutado empíricamente
- **3 race conditions auth.users**: 4 triggers AFTER INSERT inserindo em user_profiles com campos divergentes
- **1 órfã clinical_assessments TRIAGE Flávia 30/05** (<7d filtro) — será marcada `reviewed` naturalmente em ~5 dias
- **Memória `project_veredito_final_24_04` desatualizada** sobre gamification

### 🎯 Princípios meta cristalizados hoje
1. **NUNCA INSERT SQL direto auth.users** — bug Flávia → memory
2. **Mexer só no gap real** — validado 11x hoje
3. **Empírico Pedro via screenshot > Material B externo** — 8 filtros sessão
4. **Memórias estáticas envelhecem** — validar via PAT antes de afirmar números
5. **CTAs translucent emerald** = padrão app cristalizado
6. **max-w-screen-2xl** = padrão 4K/QHD pra dashboards
7. **searchParams instável em useEffect deps** causa loops invisíveis
8. **Loading state ≠ empty state** — usar mesma shape pra evitar flash

---

## 🚀 Frase âncora preventiva 01/jun

> *"31/05 fechou com 14 commits sequenciais V1.9.534-544 + bug login Flávia cravado via PAT + avaliação empírica geral revelando 5 erros nas minhas afirmações (números desatualizados de memórias 24/04 + 27/04). Sistema técnico saudável pré-PMF: 93% defesa em camadas, 100% RLS, 0 cron failures, 0 SGQ alerts, locks 8 intocados. Stripe MOCK confirmado + PaymentGate ausente = OK pré-Marco 2 (depende CNPJ João + paciente externo). Próxima sessão DEVE começar checando 01/jun 03h cron monthly-closing comportamento + 01/jun 15h decisão hard-delete 5 Edges + smoke real V1.9.533 UI Pedro. Bloqueadores 100% humanos: WhatsApps Ricardo+João+Eduardo+Claudio destravam mais que 4h código novo."*

— Setup DIARIO_01_06_2026 (próxima sessão) deve herdar carry-over deste BLOCO I integral.
