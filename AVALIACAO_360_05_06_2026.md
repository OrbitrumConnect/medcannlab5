# AVALIAÇÃO 360° DO SISTEMA — MedCannLab 3.0 (05/06/2026)

> **Método:** 5 auditorias de código em paralelo (1 por área) + 3 varreduras empíricas do banco vivo (PAT/Management API) + cruzamento com CLAUDE.md, MEMORY.md e diários (retrospectiva) + autorias externas (GPT/Claude2 citadas nos diários). Classificação 🟢 pronto · 🟡 parcial/falta-detalhe · 🟠 funciona-mas-frágil/mock/homolog · 🔴 ausente/quebrado. HEAD `05f3db9`.
>
> **Princípio honesto:** onde código e banco divergiram, o **banco (empírico) venceu**. Onde a doc divergiu do real, está sinalizado.

---

## 0. Estado vivo (snapshot empírico PAT 05/06)

| Métrica | Valor |
|---|---|
| users | 53 (5 admin · 33 `patient` · 4 `paciente` · 11 professional) ⚠️ type EN/PT misturado |
| user_roles | paciente 57 · profissional 17 · admin 5 · aluno 1 |
| clinical_reports | 151 total · 140 não-draft · **48 assinados** (signed_at) |
| aec_assessment_state | 7 COMPLETED · 8 INTERRUPTED · 3 em curso |
| clinical_rationalities | 142 (⚠️ **25 com PII residual** por heurística 3-nomes) |
| cfm_prescriptions | 54 (**8 com PDF assinado real** · 40 sem signature_timestamp · **0 com string `SIM-`**) |
| patient_exam_requests | 25 (**13 com PDF PBAD real** · 12 sem signed_at) |
| appointments | 51 scheduled · 41 cancelled · 5 completed |
| patient_documents | 8 · documents 43 · base_conhecimento 5 |
| gamification_points | 50 (cron-fed, desacoplada da UI) |
| user_subscriptions | **0** · patient_referrals **0** (pré-PMF) |
| forum_posts | 3 · courses 6 · import_batches 0 |
| **Edge Functions** | **17** (16 verify_jwt=true = **94%**; só `sign-pdf-icp` false por design) |
| **RLS** | **147/147 tabelas (100%)** · 469 policies |
| Cron jobs | 4 (video-reminders · monthly-closing[dormente] · expire-renal · sgq-health) |
| system_health_alerts abertos | **13** (7 AEC followup>5d · 6 CRON = falhas esperadas do monthly-closing dormente) |
| pki_transactions | 12 (signer_cpf placeholder pré-PMF) |

---

## 1. EIXO CLÍNICA — 🟢 sólido (núcleo é o ativo mais maduro)

| Componente | Classe | Nota |
|---|---|---|
| AEC FSM (`clinicalAssessmentFlow.ts`, 2036L) | 🟢 | 20 fases completas, persiste em DB, idempotência de report, fixes consent_given/canônico presentes. Sem stub. |
| tradevision-core (pirâmide 8 camadas, 7520L) | 🟢 | Verbatim soft+hard, Gate D' REGRA HARD §1, COS Kernel, pipeline orchestrator — instrumentado. 1 dead-code declarado (V1.9.582). |
| Relatório + RichClinicalReportView | 🟢 | Deriva de `content.*` (regra de ouro respeitada). |
| Assinatura PBAD ICP-Brasil (`sign-pdf-icp`) | 🟢 | PAdES real, lock V1.9.299. **Único Edge verify_jwt=false (por design)**. |
| **Assinatura clínica (cobertura)** | 🟠 | **Empírico:** exames 13/25 PDF PBAD real · prescrições 8/54 · 40 rx sem timestamp. `digital-signature` Edge é **simulação por default** no código (SIM-ICP-BR), MAS **0 strings SIM- no banco** → não polui; o problema é **cobertura parcial** (maioria não-assinada), não fake disseminado. |
| Agendamento + REGRA HARD §1 | 🟢 | Gate D' fail-closed, cadeia appointment→report→share. |

**Top fragilidade clínica:** dois caminhos de assinatura coexistem (`digital-signature` simulação-default vs `sign-pdf-icp` PBAD real). Prescrições não invocam o PBAD; exames sim. **Decisão pendente:** unificar no PBAD real ou cravar quando cada um vale.
**Divergência doc:** CLAUDE.md trata `digital-signature` como "ICP-Brasil/CFM 3 levels" — no código é simulação-default + integração AC real (Soluti/Certisign) em TODO comentado. O ICP **real** está só no `sign-pdf-icp`.

---

## 2. EIXO ENSINO — 🟠 estruturalmente incompleto

| Componente | Classe | Nota |
|---|---|---|
| AlunoDashboard cursos/matrículas | 🟢/🟡 | Lê tabelas reais, mas `FALLBACK_COURSE` hardcoded + stats fake (módulos/lições sempre 0, ranking `#42` literal). |
| **Teste de Nivelamento** | 🔴 | Promete "20 questões + certificado", **só abre chat**. Fachada. |
| Simulação Clínica (aluno) | 🟡 | UI rica, mas **sem scoring** — é roleplay de chat. |
| **TRL (Technology Readiness)** | 🔴 | ~7 tabelas no schema, **0 código frontend**. Confirma "TRL zerado" (depende do Eduardo). |
| Certificado de conclusão de curso | 🔴 | **Não existe.** `CertificateManagement` é cert ICP-Brasil **clínico** (médico), não acadêmico. |
| Avaliações (`useEvaluationInstruments`) | 🟢 | CRUD real publish/draft. |
| Fórum (do eixo, em Pesquisa) | 🟢 | — |

**Veredito:** eixo Ensino é o **menos pronto**. UI promete (teste/simulação/certificado) o que o backend não entrega. Anti-overclaim manda **rotular honestamente** ou construir o scoring.

---

## 3. EIXO PESQUISA — 🟢 maduro

| Componente | Classe |
|---|---|
| ForumCasosClinicos (curadoria conselho, upload, 5 racionalidades) | 🟢 |
| Terminal de Pesquisa (9 abas vivas) | 🟢 |
| Nôa Matrix Z2 (bypass FSM, modo pesquisa) | 🟢 |
| Literatura externa PubMed | 🟢 |
| **RAG dual-base** (`documents` UI vs `base_conhecimento` Edge `.limit(3)`) | 🟢 | Separação intencional anti-DOC_LIST (V1.9.318) **confirmada no código** — CLAUDE.md preciso. |

**Ressalva:** `noaResidentAI.ts` tem **duplo motor AEC** (FSM IMRE local = dead-code fallback; verdade no Edge). `noaTrainingSystem.ts` injeta mocks ("156 users", "500+ artigos") como contexto da IA → risco da Nôa afirmar número falso.

---

## 4. AUTH / RBAC / RLS / SEGURANÇA — 🟢 base sólida, 🟠 drift de fonte de role

**Fortes (🟢):** identidade só via JWT no core (nunca body); service-role **0 ocorrências no frontend**; RLS role-based moderna (V1.9.574 removeu UUIDs hardcoded); fallback seguro a `paciente`; **RLS 147/147 (100%)**; **16/17 Edges verify_jwt=true**.

**Riscos (🟠):**
1. **Fragmentação da fonte de role:** `user_roles` (canônico) vs `users.type` (sign-pdf-icp) vs `profiles.type` (**tradevision-core `:1897`**) vs `user_profiles` (renal). `profiles` tem 34 rows em drift ativo. Edge decide role de tabela não-canônica.
2. **Authz por `email.includes()`** em UI (`ChatGlobal`, `aec_gate`, `PatientAppointments`) — substring de email concede privilégio. Anti-padrão (UI-only, mas trocar por role/UUID).
3. **Dois `is_admin`**: `is_admin()` (user_roles) vs `is_admin_user()` (users.flag_admin). Risco de admin inconsistente entre policies.
4. ⚠️ **type EN/PT**: users tem 33 `patient` + 4 `paciente` — inconsistência que já mordeu (contexto bug Flávia).

---

## 5. MONETIZAÇÃO — 🟠 demo/mock end-to-end (gated Marco 1)

| Item | Classe |
|---|---|
| Checkout (PIX/cartão/boleto) | 🟠 **100% MOCK** (setTimeout + QR hardcoded) |
| PaymentGate (enforcement) | 🔴 **AUSENTE** (grep 0 matches em código) |
| Edge Stripe/checkout | 🔴 **AUSENTE** (`stripe_*` são só colunas vazias) |
| subscription_plans | 🟡 dados reais, sem cobrança |
| Wallet (split 70/30) | 🟡 RPCs reais (`ensure_wallet`/`request_payout`) gravam, mas **sem liquidação externa** |
| patient_referrals | 🟢 CRUD+consent real (0 rows) |

**Veredito:** schema+UI+contabilidade-interna prontos; **cobrança 100% mock**. Gargalo = CNPJ → Stripe Connect → PaymentGate. **Nada cobra hoje** (coerente pré-PMF).

---

## 6. IMPORT / MIGRAÇÃO DE BASE CLÍNICA — 🟢 a área nova mais madura

Motor puro (25 testes) + Wizard 6 etapas + Edge `bulk-import-emr` (v2, **provada e2e hoje** + compensação de falha parcial V1.9.592 + idempotência). **Gaps:** (a) 🟠 upload de binários 860MB (só índice/metadata hoje — v2 precisa lib de unzip + Storage real); (b) 🔴 piloto real (1.629 Eduardo) não executado — **bloqueio regulatório (CNPJ/DPA), não técnico**.

---

## 7. INFRA — misto

| Item | Classe |
|---|---|
| Email (Resend) | 🟢 produção (domínio verified) |
| Vídeo (WiseCare V4H) | 🟠 **HOMOLOG** (`homolog.v4h.cloud` no Edge) — **P0 migrar** |
| Google Calendar (sync-gcal/google-auth) | 🟠 dormindo (0 callers) |
| Storage buckets | 🟡 (binários de import pendentes) |
| Gamification | 🟡 UI lê `user_statistics`, não `gamification_points` (50 rows cron-fed, desacoplada) · cron dormente por decisão |

---

## 8. FRONTEND/UX — 🟢 landing, 🟡 padrão visual inconsistente

- Landing 🟢 (contadores ao vivo via RPC, bio Ricardo alinhada, paleta teal).
- Dashboards: ProfessionalMyDashboard/PatientDashboard 🟢; **AlunoDashboard 🟠 e TeamManagement 🟡 fora do padrão** (.app-bg-gradient + CTA translucent emerald ausentes).

---

## 9. SGQ / REGULATÓRIO / FHIR — 🟡 denso e honesto, tudo DRAFT pré-consultora

- **23 drafts SGQ** (`docs/sgq/`) cobrindo ISO 13485 / IEC 62304 / ISO 14971 / RDC657 / LGPD / RACI / rastreabilidade — todos DRAFT. Gap-analysis 02/06 honesto (PARCIAL em tudo, gargalo Marco 1 ~50%).
- **FHIR lib 🟢** (HAPI 30→0 erros, 9 testes). **🟠 Edge `fhir-export` DRIFT**: o endpoint deployado é cópia **desatualizada** — falta os 3 fixes V1.9.575 (URL absoluta + Bundle.identifier + Consent.policyRule) → **falharia o HAPI que a lib passa**. **Gap atacável #1 (risco-zero).**
- **Anti-overclaim exemplar** (preservar): nunca "FHIR Ready", nunca "não-SaMD", relatório sempre "hash SHA-256 ≠ ICP".

---

## 10. DIVERGÊNCIAS doc ↔ código ↔ banco (a corrigir no CLAUDE.md)

1. **Edges: CLAUDE.md diz 14 → real 17** (`create-patient-auth`, `fhir-export`, `bulk-import-emr` novas). verify_jwt **94% (16/17)**, não "93%".
2. **`get_chat_history` AINDA EXISTE** (v8 ACTIVE) — CLAUDE.md "em observação/hard-delete" e o agente de auditoria que disse "removida" **ambos errados**. Decidir: usar ou deletar de fato.
3. **RLS 147/147** (doc dizia 145/145) — cresceu, segue 100%.
4. **Assinatura:** `digital-signature` é simulação-default no código, mas **0 SIM- no banco**; cobertura real parcial (exames 13/25, rx 8/54). A narrativa "3 levels ICP" supervaloriza.
5. **PII residual 25 rows** (gap dizia ~20) — código corrigido (2 paths), **falta backfill do dado**.
6. **Wallet mais real** que a memória "tudo mock" sugere (RPCs gravam; falta só liquidação externa).

---

## 11. VEREDITO: o que está PRONTO, o que FALTA

### 🟢 Pronto (o coração funciona)
Núcleo clínico (AEC→Pipeline→Verbatim→Gate D'→PBAD), RAG dual-base, Fórum/Pesquisa/Matrix Z2, Auth/RLS (100%), Import (motor+Edge e2e), Landing, Email, FHIR **lib**.

### 🟡🟠 Parcial / frágil (funciona com ressalva)
Assinatura clínica (cobertura parcial + 2 caminhos), padrão visual (2 dashboards), Gamification (desacoplada), Vídeo (homolog), SGQ (drafts), drift de fonte de role.

### 🔴 Falta (ausente)
PaymentGate/Stripe, TRL, Teste de Nivelamento real, certificado acadêmico, vigilância pós-mercado ANVISA, upload binários do import.

---

## 12. BACKLOG PRIORIZADO

### Atacável JÁ (sem CNPJ, baixo risco)
1. **🟠 Edge `fhir-export` drift** → re-portar 3 fixes V1.9.575 (risco-zero, aditivo). **#1.**
2. **PII residual** → backfill 25 `clinical_rationalities` (código já corrige; só dado).
3. **Role source** → Edges usarem `get_my_primary_role`/`has_role` em vez de `profiles.type`/`users.type`.
4. **Authz `email.includes()`** → trocar por role/UUID em ChatGlobal/aec_gate/PatientAppointments.
5. **type EN/PT** → normalizar `paciente`→`patient` (ou vice) + decidir canônico.
6. **Padrão visual** → `.app-bg-gradient` + translucent emerald em AlunoDashboard/TeamManagement.
7. **Honestidade Ensino** → rotular Teste de Nivelamento/Simulação como "chat" OU construir scoring (anti-overclaim).
8. **Assinatura** → backfill `signature_timestamp` + decidir caminho único (PBAD vs digital-signature).
9. **SGQ** → promover REL-IA-001 / REL-SEC-001 de DRAFT 0.1 → auditor-ready.
10. **WiseCare homolog→produção** (P0 infra; pode não depender de CNPJ).

### Gated Marco 1 (CNPJ → DPA)
Stripe Connect → PaymentGate → monetização real · piloto import 1.629 Eduardo · petição ANVISA SaMD · vigilância pós-mercado.

### Decisões humanas (destravam ~50%)
CNPJ João Vidal · 2º médico independente · 20-30 pacientes pagantes (PMF) · TRL com Eduardo.

---

## 13. CRUZAMENTO COM RETROSPECTIVA (diários/memória)
- CLAUDE.md/MEMORY.md estão **majoritariamente precisos** sobre arquitetura (dual-base RAG, REGRA HARD, locks, Matrix Z2, TRL zerado, Stripe mock, WiseCare homolog, cron dormente). Disciplina anti-overclaim **real e rara**.
- **Stale a corrigir:** contagem de Edges (14→17), `get_chat_history` (existe), RLS (145→147), narrativa "digital-signature 3 levels" (é simulação-default), "~20 PII"→25.
- Autorias externas (GPT/Claude2) citadas nos diários **convergiram** com a análise empírica nos pontos grandes (FK auth.users, separação importado-vs-gerado, refator do Core) — sinal de diagnóstico robusto.

> **Frase âncora 360:** *"O coração (clínica + auth + RLS + import) está sólido e empíricamente verificado. As bordas — monetização, Ensino/TRL, assinatura unificada, FHIR endpoint, padrão visual — é onde mora o trabalho. Metade do roadmap está atrás de uma decisão humana (CNPJ), não de código. A doc é honesta; só precisa de um sync de números (17 Edges, RLS 147, get_chat_history vive). Nada de overclaim foi introduzido — e isso é um ativo a preservar."*
