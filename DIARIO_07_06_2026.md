# DIÁRIO 07/06/2026 — Avaliação cruzada (Claude+GPT) do diário 06/06 + calibrações empíricas

> Pedro trouxe 2 análises externas (Claude + ChatGPT) do diário 06/06. Tarefa: avaliar TUDO com a fonte de verdade (banco + código), já que só aqui há o cruzamento empírico. Foco no técnico (PAT deixado de lado a pedido do Pedro — é controlado). HEAD entrada `eb1f03c`.

## Veredito geral
As 2 análises são **afiadas e ~80% corretas**. O elogio central está certo: o ganho de 06/06 foi **disciplina de decisão** (103→6 papers, LGPD prevenido, fix renal cru evitado), não volume. A fonte de verdade **corrige 3 pontos** e **confirma o resto**.

## 🟡 Onde a FONTE DE VERDADE corrige (empírico)

**1. Sweep `#00C16A` (Calibração 4 deles) — premissa ERRADA.** Eles: *">5 ocorrências = sweep"*. **Empírico: 146 ocorrências** — mas a maioria é a **cor da marca** (`designSystem.ts`, textos, bordas, PDFs). **Sweep cego de cor QUEBRARIA a identidade visual.** O problema real é o **padrão "botão sólido gradiente + texto branco"** (poucos, já corrigidos no ClinicalReports V1.9.608), NÃO a cor. → Caçar o padrão de botão caso a caso, no olho; **nunca** sweep de cor. Calibração #4 refutada.

**2. Cron sem alerta (Calibração 3 deles) — parcialmente REDUNDANTE.** Empírico: `run_sgq_health_checks` **já tem 6 checks e JÁ monitora crons** (`ja_checa_cron=true`; o check "Cron jobs com falhas 24h" gera os 8 alertas CRON abertos = monthly-closing dormente). Então **falha** do cron renal já seria pega. Gap real = só o cenário "cron DESLIGADO/parou" (ausência ≠ falha), baixa probabilidade (scheduled+active). → Check 7 é **nice-to-have, não crítico**. Não feito (custo>benefício agora).

**3. LGPD "incidente formal" (ChatGPT) — overkill.** O doc de exame do paciente **não é vazamento ativo**: está no **Storage privado** (owner-scoped), **nunca foi indexado**. O risco era indexá-lo (evitado). Sem exposição → "incidente ANVISA/ANPD" é exagero. Correto = flag "não indexar + revisar Ricardo" (feito). A triagem **preveniu**; não houve incidente.

## 🟢 Onde eles estão CERTOS (executado hoje, zero-regressão)

**1. Autoria — domínios ≠ equivalência (a correção mais importante).** Os 2 flagaram: meu framing "3 zonas" pode soar como "Pedro pedindo autoria clínica equivalente". **Aceito.** Ricardo = autor clínico (décadas); Pedro = autor de governança cognitiva (domínio técnico distinto). **Memória ajustada** ([[project_acordo_autoria_3_zonas_co_criacao_06_06]]): apresentar a 2.1 como **reconhecimento de autoria técnica DISTINTA**, nunca "autoria clínica". Protege a reunião (evita atrito com Ricardo).

**2. Gate flag `?renal_elite=1` sem prazo.** Válido (evitar "almost-merged forever"). **Trigger documentado no componente** (`RenalSuggestionsCardElite.tsx`): POSITIVO = vira default quando Ricardo aprovar; NEGATIVO = reverter o wrapper até **13/06** se não houver decisão.

**3. DOCX backlog + boundary "condições de validade" (notas registradas):**
- **DOCX:** 27 dos 103 órfãos são `.docx`; o `extract-document-text` é **PDF-only** (pdfjs). Dívida operacional → política de ingestão futura: exigir PDF OU suportar DOCX no extrator. Não bloqueia (os 6 papers científicos eram PDF).
- **Boundary:** se `embeddings/pgvector` forem religados no futuro, **revalidar** o boundary `documents` ≠ `base_conhecimento` ≠ embeddings ANTES (condição de validade explícita). Hoje off = boundary garantido.

## 📌 Resumo executado (07/06, zero-regressão)
- ✅ Gate flag com trigger de remoção (comentário no componente)
- ✅ Memória de autoria calibrada (domínios ≠ equivalência)
- ✅ DOCX backlog + boundary condições-de-validade registrados
- ❌ Sweep #00C16A: NÃO feito (premissa errada, quebraria a marca)
- ⏸️ Check 7 cron: deixado (já coberto pela checagem genérica de falha)

## 🚀 Frase âncora 07/06 (manhã)
> *"Avaliar os avaliadores com a fonte de verdade. As 2 análises (Claude+GPT) acertaram ~80% — e o elogio central (disciplina>volume) procede. Mas o banco/código corrigiu 3: sweep #00C16A quebraria a marca (146 usos = cor, não botão), o cron já é monitorado (falha), e o doc LGPD nunca vazou (storage privado, não indexado). Aceito as 3 calibrações válidas (autoria=domínios-não-equivalência protege a reunião, gate-flag com prazo, DOCX/boundary anotados). Modelo recursivo: até análise externa entra no funil empírico antes de virar ação. Locks 8 intocados."*

---

# BLOCO B — Sidecar Neuro V1.9.611 (Fase D codada + validada E2E)

> Pedro: *"já temos o formato para neuro, só falta codar, aguardando claudi codar pro elite, não nós."* GO pra codar a Fase D (que a memória 27/05 deixou atrás do gate Eduardo). Fiz como **slug-test paralelo provado empíricamente**, gated por flag (OFF) — nada vai live sem Eduardo.

## A grande decisão arquitetural (do Pedro): fonte = RELATÓRIO, não chat cru
Comecei lendo `ai_chat_interactions`, mas bati no problema de escopo: **`session_id` é per-turno** (cada turno tem um id diferente → inútil pra agrupar a sessão AEC), e o paciente tem **283 turnos lifetime** de várias sessões/dias. Ia ter que gambiarrar janela de tempo (risco cross-session, bug 22-04).

Pedro cortou o nó: *"e pq é do relatório no caso né, mais fácil"* + *"já tá consolidado, usuário já aceitou termos."* **Certíssimo e mais correto:** o `clinical_reports.content` é **1:1 com a AEC, consolidado, consent já dado, sem problema de escopo**. A `lista_indiciaria`/`queixa`/`desenvolvimento` trazem as falas **verbatim** → `fala_literal` exata. Pivotei o Edge.

## Renal vs Neuro — achado empírico que corrige premissa
Verifiquei como o renal **realmente** funciona: a sugestão da Maria das Dores tem `parser_version='V1.9.307-retrofix-manual'` → foi **inserida à mão**, não auto-extraída. Só existe **1 `captation_extra` no banco inteiro** → o pipeline auto do renal **quase nunca fluiu**. Logo o **neuro (do report) é mais robusto que o renal** (report sempre existe; captation quase nunca). Futuro: migrar renal pra report também. Renal não tocado (zero-regressão).

## Validação empírica (o cérebro funciona)
Dry_run contra report 2bdb57fb (o do Eduardo simulando paciente burnout) → GPT-4o-mini **reproduziu o audit manual**: 3/4 falas verbatim exatas (*"dificuldade de me concentrar"*, *"...demandas como médico"*, *"burn out"*) + ainda separou ansiedade/burnout mais granular. 1 miss (*"bupropiona"*) — defensável por Z2 (inferência med→off-label é trabalho do Eduardo). RUN real: 4 criados; RUN#2: 0+4 dup → **idempotência ok**. Flag desligada de volta.

## Entregas
- **Edge `neuro-signal-extractor`** deployada, `verify_jwt=true` (smoke 401 anon). Fonte=report, GPT classifica 20 categorias (mapa Fase A 27/05), Core **intocado**.
- **Tabela `clinical_neuro_signals`** + RLS apertada (3 policies: prof SELECT+UPDATE, admin ALL — sem DELETE/INSERT pro médico) + idempotência índice completo (lição V1.9.590) + `report_id text` (gotcha: `clinical_reports.id` é TEXT).
- **4 rows reais** (passosmir4) como seed, invisíveis (card hardcoded até GO Eduardo).
- Feature flag `neuro_inline_suggestions` = **OFF** (kill switch).

## Review cruzado GPT+Claude da migration — veredito empírico
As 2 análises (~75% certas estruturalmente) **olharam só o SQL** → erraram o principal: disseram "tabela vazia/Edge não existe", quando **buildei+validei hoje** (4 rows reais). Cruzei via PAT:
- **C1 (appointments cobre neuro?)** REFUTADO: Eduardo (prof f4a62265) tem **9 appts/4 pacientes**; passosmir4 tem appt scheduled com ele → RLS mostra os sinais. "Eduardo silêncio 33d" = premissa errada.
- **Especialidade (GPT)** VÁLIDO → comment corrigido (RLS é appointment-based, não especialidade).
- **FOR ALL/DELETE (Claude)** VÁLIDO → apertei RLS (prof sem DELETE).
- **WITH CHECK (Claude)** MOOT (Postgres usa USING implícito) → adicionei explícito mesmo assim.
- **CHECK epilepsia/TOC (C2)** parcial (2 reports c/ convulsão real) mas o prompt só emite 3 → CHECK nunca quebra; 'OUTROS' deferido Eduardo.
- **Drift subcategoria** baixo (prompt=soft-enum, 4 rows limpas) → comment + hard-enum V1.9.612.
- **Log vs estado** recomendo LOG (report-âncora=longitudinal) → decisão Eduardo.

## Card REAL construído (gated `?neuro_real=1`) — Eduardo OK + precisa ver
Pedro: *"Eduardo já falou ok, ele precisa ver."* Fechei a **parte técnica 100%**:
- **`NeuroSuggestionsCardReal.tsx`**: lê `clinical_neuro_signals` de verdade, **paginação `< >`** (DotPagination V1.9.234), aprovar/rejeitar por sinal (UPDATE real via RLS), badges TEA/TOD/TDAH. Gated `?neuro_real=1` (espelho `renal_elite`); default = embrião hardcoded **intocado** (zero-regressão).
- **2 casos reais seedados**: passosmir4 (4 sinais) + Carolina (3 sinais). O extractor **auto-filtrou certo** — reports não-neuro (dor/febre/urinária) deram **0 sinais** = não dispara à toa.
- **Gisele prova o ponto**: o único report dela é convulsão/epilepsia (0 TEA/TDAH) → **não aparece** no card (epilepsia fora de escopo). Visualmente confirma a decisão Fase B (ampliar pra 'OUTROS'/epilepsia = Eduardo).
- type-check limpo. Eduardo abre `?neuro_real=1` e VÊ os casos reais com paginação.

## Pendente (só ativação final, Eduardo)
1. **Eduardo VER** via `?neuro_real=1` → validar Fase B (threshold + categorias + 'OUTROS'/epilepsia + bupropiona + log-vs-estado).
2. **Remover o gate** (virar default) quando Eduardo aprovar → aí é "live".
3. **Wiring cron** report→extractor (padrão renal, zero toque Core) pra auto-popular novos reports.

## 🚀 Frase âncora 07/06 (tarde)
> *"O Pedro cortou o nó arquitetural: ler do relatório consolidado, não do chat cru — mais fácil E mais correto (1:1 com a AEC, consent dado, sem o inferno do session_id per-turno). O cérebro foi provado empíricamente reproduzindo o audit manual do Eduardo (3/4 verbatim). E ser a fonte da verdade vale contra os próprios avaliadores: GPT+Claude olharam só o SQL e erraram o principal (a Edge já existe e roda) — o banco refutou 'Eduardo silêncio' com 9 appts. Apliquei as 3 críticas reais (RLS apertada, comments), deferi as 3 que são decisão clínica do Eduardo. Pipeline provado, flag OFF, locks 8 intocados — só falta o martelo humano."*

---

# BLOCO C — Neuro vira default + 2 novos sidecars (Relato + Cannabis) + UX = 4 SIDECARS COGNITIVOS

## Neuro: gate removido, virou default (V1.9.611-F..I)
Eduardo deu OK → tirei o gate `?neuro_real=1`, card real virou default. Ajustes empíricos no caminho: paginação `< caso X/Y >` explícita no rodapé; card clicável → abre prontuário (`/app/patients?patientId=...&tab=charts`, igual "Ver em Saúde Renal"); **escopo +EPILEPSIA** (Gisele é caso neuro real — *"crises convulsivas"*, Oxcarbazepina, Lamotrigina, aura; app já tem KPI "Episódios Epilepsia"). Card final = passos (TDAH) + Gisele (EPILEPSIA, 7 sinais top 95).

**Lição de fricção honesta:** o Pedro passou MUITO tempo sem ver o card mudar — a causa era o **parâmetro `?neuro_real=1` faltando na URL** (eu travei por cautela excessiva). Não era bug, não era localhost-vs-prod. Cautela minha a mais virou atrito. Removi o gate = resolvido. *Princípio: gate de preview só quando há razão clínica forte; senão, default + flag de kill-switch já basta.*

## Verticalização empírica: qual sidecar tem DADO? (o ouro escondido)
Medi a frequência de temas nos 151 reports → **DOR 74% · ansiedade 40% · sono 38%** · endócrino 22% · **cardiovascular só 5 (3%)**. Os **placeholders existentes (Cardiovascular/Endócrino) eram onde MENOS tinha dado** — eu ia construir num slot vazio. O ouro estava em dor/sono/ansiedade (as 3 indicações centrais da cannabis) — não viradas sidecar.

## V1.9.612 — "Sinais do Relato" (DOR · SONO · ANSIEDADE)
Pedro recusou os nomes "Indicações"/"Sugestões" (sugerem o APP indicando = viola Z2). A verdade: é a **fala espontânea do paciente, captada e repassada** (paciente→app→pro, MIMRE). Nome cravado: **"Sinais do Relato"**. Mesma arquitetura provada (Edge report-signal-extractor + tabela clinical_reported_signals + card teal). 22 categorias. Substituiu Cardiovascular. Seed: 4 casos (Carolina/Cristiano/Maria/passos) DOR+SONO.

## V1.9.613 — "Cannabis no Relato" (VONTADE · USO · RECEIO)
Pedro: *"seria errado identificar como o Cristiano falou? é o relato do usuário, vontade/curiosidade, trazido ao profissional na fala. Como saber se não por um card? E os que aderiram = eficácia."* **Certíssimo.** Eu tinha sido cauteloso ("dado fino") com base num **keyword raso (1/2/0)** — mas o **GPT semântico achou sinais reais em ~6 pacientes** (Cristiano *"quero tomar cbd"*, passos *"substituir por algo natural"* + *"óleo de cannabis"*, Maria *"iniciei uso de CBD"*, Carolina, Pedro Paciente). **Refutou minha cautela.** Card lime, substituiu Endócrino. Fase 2 parqueada: cruzar com prescrições (adesão→eficácia).

## UX do painel "Analisar Paciente" (V1.9.611-H/I)
Drawer colado na lateral → **modal centralizado 2 colunas** (Sinopse full-width + side-by-side). Card "Analisar Paciente" `flex-1` esticado → **~50% (max-w-md)**.

## 🚀 Frase âncora 07/06 (noite) — a proposta virou realidade
> *"4 SIDECARS COGNITIVOS REAIS num dia: Renal (Ricardo/nefro) · Neuro (Eduardo) · Sinais do Relato · Cannabis no Relato. O app verticalizou no eixo rim↔cannabis com UMA arquitetura provada (Edge classifica o report consolidado → tabela → card), trocada só pelo MAPA. A ideia central — relato espontâneo do paciente (MIMRE) entregue fielmente ao médico (Z2: sinaliza, não decide) — virou superfície funcional. LIÇÃO DA NOITE: keyword subconta o semântico (probe raso quase me fez parquear o cannabis-relato válido; o GPT provou que o dado estava lá). E os placeholders (Cardiovascular/Endócrino) eram slots vazios — construir onde o DADO está, medido empíricamente, não onde o roadmap chutou. Locks 8 intocados. Sem regressão. A dignidade da ideia preservada: o app escuta e repassa, não inventa."*
