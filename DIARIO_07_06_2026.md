# DIÁRIO 07/06/2026 — Avaliação cruzada (Claude+GPT) do diário 06/06 + calibrações empíricas

> Pedro trouxe 2 análises externas (Claude + ChatGPT) do diário 06/06. Tarefa: avaliar TUDO com a fonte de verdade (banco + código), já que só aqui há o cruzamento empírico. Foco no técnico (PAT deixado de lado a pedido do Pedro — é controlado). HEAD entrada `eb1f03c`.

## Veredito geral
As 2 análises são **afiadas e ~80% corretas**. O elogio central está certo: o ganho de 06/06 foi **disciplina de decisão** (103→6 papers, LGPD prevenido, fix renal cru evitado), não volume. A fonte de verdade **corrige 3 pontos** e **confirma o resto**.

## 🟡 Onde a FONTE DE VERDADE corrige (empírico)

**1. Sweep `#00C16A` (Calibração 4 deles) — premissa ERRADA.** Eles: *">5 ocorrências = sweep"*. **Empírico: 146 ocorrências** — mas a maioria é a **cor da marca** (`designSystem.ts`, textos, bordas, PDFs). **Sweep cego de cor QUEBRARIA a identidade visual.** O problema real é o **padrão "botão sólido gradiente + texto branco"** (poucos, já corrigidos no ClinicalReports V1.9.608), NÃO a cor. → Caçar o padrão de botão caso a caso, no olho; **nunca** sweep de cor. Calibração #4 refutada.

**2. Cron sem alerta (Calibração 3 deles) — parcialmente REDUNDANTE.** Empírico: `run_sgq_health_checks` **já tem 6 checks e JÁ monitora crons** (`ja_checa_cron=true`; o check "Cron jobs com falhas 24h" gera os 8 alertas CRON abertos = monthly-closing dormente). Então **falha** do cron renal já seria pega. Gap real = só o cenário "cron DESLIGADO/parou" (ausência ≠ falha), baixa probabilidade (scheduled+active). → Check 7 é **nice-to-have, não crítico**. Não feito (custo>benefício agora).

**3. LGPD "incidente formal" (ChatGPT) — overkill.** O doc de exame do paciente **não é vazamento ativo**: está no **Storage privado** (owner-scoped), **nunca foi indexado**. O risco era indexá-lo (evitado). Sem exposição → "incidente ANVISA/ANPD" é exagero. Correto = flag "não indexar + revisar Ricardo" (feito). A triagem **preveniu**; não houve incidente.

## 🟢 Onde eles estão CERTOS (executado hoje, zero-regressão)

**1. Autoria — domínios ≠ equivalência (a correção mais importante).** Os 2 flagaram: meu framing "3 zonas" pode soar como "Pedro pedindo autoria clínica equivalente". **Aceito.** Ricardo = autor clínico (décadas); Pedro = autor de governança cognitiva (domínio técnico distinto). **Memória ajustada** ([[project_acordo_autoria_3_zonas_co_criacao_06_06]]): apresentar a 2.1 como **reconhecimento de autoria técnica DISTINTA**, nunca "autoria clínica". Protege a reunião (evita atrito com Ricardo).

**2. Gate flag `?renal_elite=1` sem prazo.** Válido (evitar "almost-merged forever"). **Trigger documentado no componente** (`RenalSuggestionsCardElite.tsx`): POSITIVO = vira default quando Ricardo aprovar; NEGATIVO = reverter o wrapper até **13/06** se não houver decisão.

**3. DOCX backlog + boundary "condições de validade" (notas registradas):**
- **DOCX:** 27 dos 103 órfãos são `.docx`; o `extract-document-text` é **PDF-only** (pdfjs). Dívida operacional → política de ingestão futura: exigir PDF OU suportar DOCX no extrator. Não bloqueia (os 6 papers científicos eram PDF).
- **Boundary:** se `embeddings/pgvector` forem religados no futuro, **revalidar** o boundary `documents` ≠ `base_conhecimento` ≠ embeddings ANTES (condição de validade explícita). Hoje off = boundary garantido.

## 📌 Resumo executado (07/06, zero-regressão)
- ✅ Gate flag com trigger de remoção (comentário no componente)
- ✅ Memória de autoria calibrada (domínios ≠ equivalência)
- ✅ DOCX backlog + boundary condições-de-validade registrados
- ❌ Sweep #00C16A: NÃO feito (premissa errada, quebraria a marca)
- ⏸️ Check 7 cron: deixado (já coberto pela checagem genérica de falha)

## 🚀 Frase âncora 07/06
> *"Avaliar os avaliadores com a fonte de verdade. As 2 análises (Claude+GPT) acertaram ~80% — e o elogio central (disciplina>volume) procede. Mas o banco/código corrigiu 3: sweep #00C16A quebraria a marca (146 usos = cor, não botão), o cron já é monitorado (falha), e o doc LGPD nunca vazou (storage privado, não indexado). Aceito as 3 calibrações válidas (autoria=domínios-não-equivalência protege a reunião, gate-flag com prazo, DOCX/boundary anotados). Modelo recursivo: até análise externa entra no funil empírico antes de virar ação. Locks 8 intocados."*
