# Auditoria empírica 06/05/2026 — pós-V1.9.170 + V1.9.169-B

**Contexto:** Após executar pacote V1.9.163-170 (8 commits, ~200 LOC). Pedro pediu *"auditoria do que precisar supa+front+back para entender melhor ainda o app"*. Auditoria 100% empírica via PAT (`sbp_5657...`) + type-check + git log. Princípio META-2 aplicado: gaps declarados explicitamente no fim.

---

## 1. Backend — Supabase contagens (snapshot 06/05 17h30 BRT)

### 1.1 Estrutura geral

| Métrica | Valor | Δ vs CLAUDE.md (28/04) |
|---|---|---|
| Tabelas em `public` (BASE TABLE) | **135** | era 130+ → **+5** |
| Triggers user-level | **82** | era 76+ → **+6** |
| Functions em `public` | **342** | era 80+ RPCs → **muito maior** (inclui internas) |
| Edge Functions ACTIVE | **10** | conforme CLAUDE.md |
| pg_cron jobs ACTIVE | **2** | `video-call-reminders-5min` + `monthly-closing-medcannlab` |

### 1.2 Edge Functions (todas ACTIVE, por slug)

```
digital-signature                v 58   ACTIVE   (era v52, +6)
extract-document-text            v 55   ACTIVE   (era v49, +6)
get_chat_history                 v  7   ACTIVE   (era v6, +1)
google-auth                      v 22   ACTIVE   (era v16, +6) ← evoluiu
send-email                       v 54   ACTIVE   (era v46, +8)
sync-gcal                        v 22   ACTIVE   (era v16, +6) ← evoluiu
tradevision-core                 v333   ACTIVE   (era v302, +31) ← muita atividade
video-call-reminders             v 19   ACTIVE   ← V1.9.163 hoje (era v3 desktop)
video-call-request-notification  v 55   ACTIVE   (era v49, +6)
wisecare-session                 v 74   ACTIVE   (era v68, +6)
```

**Achado:** `tradevision-core` saltou v302 → v333 (31 deploys). Aumento previsível de atividade clínica/IA.

### 1.3 Realtime publication `supabase_realtime` (12 tabelas)

```
ai_chat_interactions      chat_messages_legacy    notifications ← V1.9.164
appointments              chat_participants       user_courses
chat_messages             chat_rooms              user_profiles
                          global_chat_messages    video_call_requests
                          noa_interaction_logs
```

**V1.9.164 confirmado:** `notifications` está na publication. Sino realtime do paciente recebe INSERT em tempo real.

### 1.4 Métricas de entidades-chave

| Tabela | Total | Recente (24-48h) | Observação |
|---|---|---|---|
| `users` | 40 | — | distribuído por type (ver 1.5) |
| `appointments` | 76 | 4 (em 48h) | distribuído por status (ver 1.6) |
| `clinical_reports` | 98 | 2 (em 48h) | atividade clínica baixa, esperado pré-PMF |
| `clinical_assessments` | 61 | 2 (em 48h) | AEC FSM ativa |
| `clinical_rationalities` | 81 | — | pipeline ativo (acumulado) |
| `clinical_axes` | 380 | — | múltiplos eixos por avaliação |
| `cfm_prescriptions` | 37 | — | distribuído por status (ver 1.7) |
| `notifications` | 154 | 20 (em 24h) | **115 unread** ⚠️ |
| `video_call_requests` | 239 | 19 (em 48h) | volume alto de tentativas |

### 1.5 ⚠️ ACHADO: enum `users.type` mistura PT/EN

```
patient        22  ← INGLÊS (maioria)
professional   10  ← INGLÊS (maioria)
admin           5
paciente        3  ← PORTUGUÊS (legacy/CLAUDE.md)
```

**Implicação:** CLAUDE.md afirma 4 enums em `app_role` PT (`paciente`, `profissional`, `admin`, `aluno`) mas a tabela `users.type` está usando EN como padrão atual. Possível divergência entre `app_role` e `users.type`. **Não é bug, é divergência semântica documentada que vale checar onde cada um é usado.**

### 1.6 Status de `appointments`

```
cancelled    37   (data: 28/01 a 03/12/2026)
scheduled    35   (data: 28/01 a 22/05/2026)
completed     4   (data: 24/04 a 04/05/2026) ← consultas reais
```

**Próximas confirmadas (futuro):** próxima é 2026-05-20 (14 dias). Cron `video-call-reminders-5min` está ativo mas reminders no horizonte 24h-1min ainda não dispararam (`reminded_24h=0`, `reminded_1min=0`) — não é bug, é falta de tráfego no curto prazo.

### 1.7 ⚠️ ACHADO: gargalo de assinatura CFM persiste

```
cfm_prescriptions:
  draft     35  (94.6%)  ← gargalo confirmado
  signed     1
  sent       1
```

**Status:** confirmação do achado documentado em [project_analytics_share_04_05.md] — quase 95% das prescrições ficam DRAFT, sem chegar a `signed`. Hoje só 2 prescrições saíram de DRAFT.

### 1.8 Tabelas P0 half-implemented — STATUS ATUALIZADO

| Tabela | Existe? | Rows | Antes (CLAUDE.md) |
|---|---|---|---|
| `integration_jobs` | ✅ SIM | **0** | "tabela ausente, P0" |
| `professional_integrations` | ✅ SIM | **0** | "tabela ausente, P0" |
| `imre_assessments` | ❌ NÃO | — | 1 dos 4 motores dormentes |
| `imre_semantic_blocks` | ❌ NÃO | — | 1 dos 4 motores dormentes |
| `imre_semantic_context` | ❌ NÃO | — | 1 dos 4 motores dormentes |
| `clinical_integration` | ❌ NÃO | — | mencionado em código legacy |

**Achado significativo:** P0 google-auth + sync-gcal **não está mais "tabela faltando"** — agora é **"tabela existe, zero uso"**. Essas Edge Functions podem rodar sem erro, mas ninguém as está chamando ainda. Status real mudou de 🔴 P0 → 🟡 latente.

### 1.9 ⚠️ ACHADO: 115 notificações unread

`notifications.is_read=false` total **115** — pacientes não estão marcando notificações como lidas. Possíveis causas:
1. Sino não estava entregando (V1.9.164 acabou de ligar — pré-fix, paciente nem via)
2. Falta UX clara de "marcar como lida"
3. Notificações geradas em massa sem visualização

**Não bloqueante**, mas deve regredir naturalmente nos próximos dias com sino + som funcionais (V1.9.164 + V1.9.165).

---

## 2. Frontend — type-check + estado código

### 2.1 Type-check baseline

**34 erros, distribuídos em 6 arquivos — todos CLUSTER IMRE LEGACY (não motores dormentes):**

| Arquivo | Erros | Causa |
|---|---|---|
| `src/components/SlidePlayer.tsx` | 1 | `dompurify` ausente (gotcha conhecido CLAUDE.md) |
| `src/lib/clinicalAssessmentService.ts` | ~5 | tabela `imre_assessments` (cluster IMRE morto) |
| `src/lib/imreMigration.ts` | ~13 | tabelas `imre_*` (cluster IMRE morto) |
| `src/lib/noaIntegration.ts` | ~4 | tabelas `imre_*` (cluster IMRE morto) |
| `src/lib/unifiedAssessment.ts` | ~9 | `imre_*` + `clinical_integration` (cluster IMRE morto) |
| `src/pages/ClinicalAssessment.tsx` | 2 | `imre_assessments` + `clinical_integration` (cluster IMRE morto) |

**Clarificação crítica (Pedro 06/05):** **AEC é o sistema operacional REAL**. IMRE foi nomenclatura semântica/educacional cunhada pela IA na fase de fundação (fev/2026), tentou virar estrutura paralela (tabelas `imre_*` + 4 arquivos service), banco rejeitou (tabelas dropadas), Dr. Ricardo retratou nomenclatura 24/04. **Frase âncora:** *"AEC executa. IMRE descreve."*

Os 34 erros são **fantasma materializado** ainda no repo, aguardando **Caminho A** (recomendado alinhado com P8): deletar 4 arquivos legacy + refatorar `ClinicalAssessment.tsx` para usar `clinical_assessments` (tabela AEC real). Decisão pendente Ricardo. **Nenhum erro nos arquivos do pacote V1.9.163-170**. Baseline preservado.

Ver [project_aec_vs_imre_clarificacao.md] na memória persistente para histórico completo.

### 2.2 Estado git

```
HEAD:        201c8d0 (V1.9.169-B URGENTE — fix mobile chat scroll)
Branch:      fix/v1.9.92-remover-consent-rota-fantasma
Tag stable:  v1.9.113-locked (preservada)
Sync:        local = hub = origin (push 4 refs em todos os 8 commits)
```

### 2.3 Smoke estimado dos componentes V1.9.163-170

| Versão | Smoke validável local | Smoke validável produção |
|---|---|---|
| V1.9.163 email enriquecido | ❌ (precisa Resend prod + appointment 24h) | aguarda próxima consulta marcada |
| V1.9.164 sino realtime | ❌ (precisa INSERT real) | aguarda evento real |
| V1.9.165 som | ✅ trigger manual via Console | smoke próxima notificação |
| V1.9.166 flip câmera | ✅ mobile dev | smoke próxima videoconsulta |
| V1.9.168 banner colapsável | ✅ visual | smoke usuário |
| V1.9.169-B scroll mobile | ✅ Pedro já validou | smoke confirmado mobile |
| V1.9.170 Canal Atendimento | ✅ visual | smoke usuário |

---

## 3. Síntese — saúde do app em 5 dimensões

### 3.1 🟢 LOCK CORE intocado
- AEC FSM ativa (61 assessments, 2 nas últimas 48h)
- Pipeline rationalities/axes ativo (81/380 rows)
- Tag `v1.9.113-locked` preservada em git
- Edge tradevision-core estável (v333 sem hotfix)
- REGRA HARD §1 preservada

### 3.2 🟢 INFRAESTRUTURA RECENTE FUNCIONANDO
- Resend produção (V1.9.99-B + V1.9.163 v19)
- pg_cron 2 jobs ativos
- Realtime 12 tabelas (notifications adicionada V1.9.164)
- Marketplace layer (V1.9.150-156, ativado)
- Dual provider videoconsulta (WiseCare + WebRTC P2P)

### 3.3 🟡 BACKLOG TÉCNICO conhecido (sem regressão nova)
- 34 erros TS legacy nos 4 motores dormentes (não bloqueia build Vercel)
- 115 notifs unread (auto-regride com V1.9.164+165)
- 35 cfm_prescriptions em DRAFT (gargalo crônico)
- `dompurify` ausente local (Vercel ok)

### 3.4 🟡 P0 atualizados de status
- `integration_jobs` + `professional_integrations` **EXISTEM agora** com 0 rows → mudou de 🔴 P0 → 🟡 latente (esperando ativação real)
- google-auth v22 + sync-gcal v22 deployadas, sem uso real ainda

### 3.5 🟡 Pendências validação humana
- Smoke V1.9.163-170 próxima consulta Ricardo + paciente real
- V1.9.167 layout split (~45 LOC, polish)
- Bug Carolina state Fix #1 SQL (~5 min, autorização Pedro)
- V1.9.121 fases 3-6 (~4h, OK Ricardo)
- CNPJ/Cert ICP-Brasil (humanos)

---

## 4. Gaps declarados (META-2)

Esta auditoria **NÃO cobriu**:

1. **RLS policies** das 135 tabelas — só checamos publication. Não verifiquei se cada policy continua coerente após V1.9.164 (apenas SQL ALTER PUBLICATION foi aplicado, deveria ser inocuo).
2. **Edge function logs runtime** — não acessei `analytics.function_logs` para confirmar 0 erros pós-deploy V1.9.163 v19. Apenas confirmei `status=ACTIVE`.
3. **Storage buckets** — não auditei estado dos buckets (`documents`, `chat-images`, etc). Pode haver órfãos novos pós-28/04.
4. **Auth users vs public.users sync** — não verifiquei se 40 users em `public` casa com `auth.users`.
5. **Performance** (índices, slow queries) — fora do escopo desta sessão.
6. **Coverage smoke V1.9.163-170** — só V1.9.169-B foi validado empiricamente em mobile. Os outros 7 esperam smoke real.
7. **8 sócios CNPJ / Triple-A V1.9.140-C / Marketplace V1.9.150-156** — assumi documentação dos diários como verdadeiros sem revalidar de zero.
8. **Carolina state ATIVO** — não rodei query do bug pendente, só registrei como ainda em backlog.

---

## 5. Frase âncora da auditoria

> *"App em estado saudável: 10 Edge Functions ACTIVE, 135 tabelas, 82 triggers, 2 cron jobs, 12 tabelas realtime. Pacote V1.9.163-170 100% deployado sem regressão em CORE. 34 erros TS são legacy nos 4 motores dormentes (não bloqueiam Vercel). P0 google-auth/sync-gcal **mudou de status**: tabelas existem agora, esperando ativação. Gargalo crônico cfm_prescriptions DRAFT 94.6% persiste. 115 notifs unread devem auto-regredir com sino+som ativos. Lock V1.9.95+97+98+99-B intocado. Próximo gate: smoke próxima consulta marcada."*

---

*Auditoria executada por Claude Opus 4.7 (1M ctx) via PAT `sbp_5657...` Management API. 06/05/2026 ~17h30 BRT.*
