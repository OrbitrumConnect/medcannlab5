# DIÁRIO 05/06/2026 — Migração de Base Clínica: wizard (Fase 3) + piloto e2e que provou a Edge

> Continuação do marco de importação (Fases 0-2 fechadas em 04/06). Hoje: UI wizard + o piloto e2e que faltava — rodar a Edge `bulk-import-emr` **de verdade** com dados sintéticos, provar idempotência, achar os bugs que só aparecem ponta-a-ponta, e limpar tudo.

## Sequência (HEAD `cfbc6c9`)

| V | O quê | Commit |
|---|---|---|
| **V1.9.589** | Wizard UI (6 etapas) liga ao Edge bulk-import-emr | `ac309dd` |
| **V1.9.590** | Índices idempotência PARCIAIS → COMPLETOS (achado piloto) | `9841672` |
| **V1.9.591** | normalizeGender → M/F/Outro (formato canônico do app) | `cfbc6c9` |
| **V1.9.592** | Edge compensa falha parcial (deleta identidade órfã) | `1cd4605` |

## BLOCO A — Wizard (V1.9.589)
`ImportClinicalBaseWizard.tsx` (modal próprio, estilo do app — projeto não tem shadcn Dialog) no menu *Novo Paciente* do `PatientsManagement`. 6 etapas: Termo (aceite gravado com versão+hash SHA-256+UA em `import_batches`) → Upload multi-CSV → Análise (motor puro, nada gravado) → Mapeamento (transparência origem→destino) → Revisão → Importar (cria lote + POSTa em **chunks de 50 SEQUENCIAIS**, cada chunk leva os filhos dos seus pacientes → sem race no progresso). v1 = multi-CSV (sem dep de zip; xlsx já cobre RFC-4180). Isolado: nada acontece sem upload+termo+clique. type-check 0 + 25 testes + build OK.

## BLOCO B — Piloto e2e: a Edge funciona ponta-a-ponta ✅
Profissional + 2 pacientes **sintéticos** (criados via `auth.admin.createUser` — jeito sancionado, nunca SQL direto), JWT real de usuário (a Edge é verify_jwt=true), 3 execuções do mesmo lote, **cleanup total verificado (0 resíduo, auth removido, segredos temp apagados)**.

Provado empíricamente: cria `auth.users`+`public.users` (FK satisfeita) · proveniência (source_external_id namespaced + import_batch_id) · visibilidade (patient_professional_links) · filhos → patient_medical_records (evolution/prescription_history/exam_history) · **idempotência** (3 runs → 2 pacientes / 3 filhos estáveis, sem duplicar) · dedup por CPF (re-run = merge) · órfão pulado.

## BLOCO C — 2 bugs que só o e2e pega (testes unitários não pegariam)

**A — Índices de idempotência PARCIAIS quebravam ON CONFLICT (V1.9.590).** Os índices do V1.9.587 eram `WHERE source_external_id IS NOT NULL`. O `supabase-js .upsert({onConflict})` gera `ON CONFLICT (col)` sem o predicado WHERE → Postgres não casa com índice parcial → "no unique constraint matching ON CONFLICT" → **todo insert de filho falhava**. Fix: índice unique COMPLETO (em coluna nullable, NULLs são distintos → nativos não colidem mesmo assim). Aplicado via PAT + migration no repo + reprovado e2e.

**B — Falso alarme honesto (corrigido em tempo real).** Primeiro diagnostiquei "duas CHECK constraints de gender contraditórias app-wide". **Errado** — foi artefato de query `WHERE conname='users_gender_check'` SEM filtro por tabela, que varreu a constraint homônima de uma tabela de **BACKUP** (`backup.backup_20251110_...`). A `public.users` viva tem **uma só** constraint: `gender IN (M,F,Outro)` (validada, igual ao que NewPatientForm grava). Pedro mandou *"investigar antes de tocar constraint"* — e a investigação mostrou que **não havia nada pra dropar**. O único problema real era o **meu mapper** emitindo `male/female/other` (formato do backup, não do app). Fix V1.9.591: `normalizeGender` → `M/F/Outro`.

## BLOCO D — Lições
1. **O piloto e2e justificou-se sozinho:** os 2 achados (índice parcial, formato de gender) são invisíveis a type-check/unit test — só aparecem com a Edge gravando de verdade. Mesma lição dos achados de 04/06 (FK auth.users, .limit(20)).
2. **Investigar antes de tocar (instinto do Pedro) evitou um DROP errado:** eu ia tratar uma constraint de backup como bug de produção. Filtrar `conrelid` por tabela desfez o falso alarme. Query de catálogo SEM filtro de tabela = pega homônimos de outros schemas (backup/legacy).
3. **Retorno de `.upsert` conta linhas *tentadas*, não *inseridas*** (com ignoreDuplicates) — idempotência só se prova **contando linhas persistidas via PAT**, não pelo retorno da função.

## BLOCO E — 3º achado: falha parcial deixava órfão (V1.9.592)
O Pedro flagrou 2 "Paciente Piloto" ainda no sistema — resíduo da run r1 (a que falhou no gender). **Causa real:** `createUser` é não-atômico com o `UPDATE` seguinte. No r1, o createUser criou a identidade (auth + public via trigger), mas o UPDATE falhou no gender → identidade **órfã SEM source_external_id** → invisível ao meu cleanup-por-proveniência. Removidos os 2 (public+auth). **Blindagem V1.9.592:** no erro do UPDATE, a Edge deleta a identidade recém-criada (best-effort, public+auth) antes do throw. Smoke: gender inválido de propósito → `errors:1` + **0 órfãos** (antes: 2). deno check limpo, deploy v2 verify_jwt=true preservado. Lição: **operação multi-passo em produção precisa de ação compensatória no catch** — senão falha parcial vira lixo silencioso.

## 🚀 Frase âncora 05/06
> *"A Edge mais sensível da feature (cria identidade auth REAL) foi provada ponta-a-ponta com sintéticos e limpa sem resíduo — e o piloto pagou-se achando 2 bugs invisíveis a teste unitário. Um falso alarme (constraint de backup confundida com produção) foi desfeito investigando antes de tocar, exatamente como o Pedro pediu. Locks 8 intocados. Import: motor ✅ + Edge ✅ + wizard ✅ + e2e ✅. Falta piloto com volume real (gated Marco 1 CNPJ/DPA) + arquivos 860MB."*
