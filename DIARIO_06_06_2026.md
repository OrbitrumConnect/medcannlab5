# DIÁRIO 06/06/2026 — Fase A Matrix Z2: curadoria empírica dos 103 órfãos (pré-indexação)

> Continuação da madrugada 05→06/06. Foco: aplicar o modelo triple-A sem regressão pra **avaliar a Fase A** (indexar corpus do Ricardo) ANTES de tocar qualquer coisa. HEAD entrada `448cfcb`.

## 🎯 Objetivo
Pedro autorizou começar os ataques cirúrgicos. Antes de indexar os "103 órfãos" (do checklist da reunião), aplicar "checar empiricamente + os porquês" pra descobrir **o que realmente são** e se há **pessoal do Ricardo que não deve subir**.

## BLOCO A — Onde estão os 103 (storage vs catálogo)
2 lugares: **Storage** (nuvem Supabase, bucket `documents` privado, 139 arquivos, 120,6 MB = os binários) vs **`public.documents`** (catálogo, 43 rows = as fichas que o app lê). Cruzamento por `file_url`: 36 indexados + **103 órfãos** (binário no Storage SEM ficha). Donos: Ricardo 38 (20 prof + 18 admin), Pedro 62, Eduardo 3. **0 sem-dono** → refuta medo de órfão-de-owner-deletado.

## BLOCO B — Boundary cravado (3 bases, não 2)
Lendo o Edge: os ~10 reads de `documents` são a feature **DOC_LIST** (listar/buscar doc, gated por `target_audience` pra paciente), NÃO injeção no raciocínio AEC. O **RAG clínico/Matrix** = `base_conhecimento` (5 curadas, linha ~4836), separado. Embeddings/pgvector **off**. → **Indexar em `documents` com `target_audience=['professional']` não contamina AEC nem muda experiência do paciente** (respeita lock V1.9.318). `isLinkedToAI` é flag de frontend (Edge não lê).

## BLOCO C — Triagem: 103 → ~17 papers reais
O "indexar 103" do checklist **seria erro**. Composição real:
- ✅ **~17 papers científicos** (nefro/cannabis/IA/entrevista) = corpus real
- 🔁 duplicatas massivas (OMS 8× · Catálogo 6× · reclassificação 5× · global burden 4×)
- 📄 **27 DOCX** (extract-document-text é PDF-only → fora do índice automático)
- 🟡 internos/operacionais (catálogos REUNI, planilhas, COAs, configs)
- ❌ outro projeto (Dengue/Jardins de Cura) + sem-correlação (TikTok Creator Academy, Hall cultural identity, musical tempo)

## BLOCO D — 🔴 Achado sensível (o porquê que Pedro pediu)
- **`SOLICITAÇÃO_DE_EXAME...dismorf`** (2×) = **pedido de exame de paciente individual** no acervo institucional → **vazamento LGPD**, quarentenar (precedente `cases_lgpd_quarantine`).
- **CV Lattes Ricardo**, **Dossiê de Obra Intelectual** (IP do método), **WhatsApp image**, **Relatório CDV Sopro** = pessoais/autoral → não subir sem aval Ricardo.

## BLOCO E — Deliverable + gate
`docs/CURADORIA_CORPUS_RICARDO_FASE_A_06_06.md` (read-only): 17 a aprovar + bloqueados com motivo + garantias de não-regressão. **Nada indexado** — aguarda GO Ricardo (corpus dele + confirmação dos sensíveis). Após GO: extract-document-text → `documents` professional-scoped. Memória: `project_matrix_z2_fase_a_curadoria_103_orfaos_06_06`.

## BLOCO F — Fase A.1 EXECUTADA (piloto → escala), dedup salvou de criar lixo
Após GO do Pedro (papers que Ricardo subiu PRA plataforma = base, mesmo subidos por ele; só paciente/gerado fica fora). Mecanismo decifrado: divisão Curadoria vs Meus Documentos = flag **`is_curated`** (V1.9.560-B); front re-assina URL a partir do path (Library.tsx:1571); trigger `documents_set_uploaded_by` preserva dono se explícito.

**Piloto (2):** global burden + Tangri → signed URL 30d + INSERT `is_curated=true`+`target_audience=['professional']`+`uploaded_by`=dono. Verificado e2e: base_conhecimento intocada, paciente não vê, HTTP 206 (link abre).

**🚨 Dedup-vs-catálogo antes de escalar:** revelou que a MAIORIA dos "17 papers" **já estava indexada** (os órfãos são cópias duplicadas dos 36) — bulk cego criaria ~10 duplicatas. **Só 6 papers genuinamente novos:** global burden, Tangri, Mycophenolate, ho-et-al cannabis CKD, Finerenone, Towards conversational AI. Os demais = duplicatas/internos/Dengue/docx/**1 doc paciente LGPD**.

**Resultado:** documents 43→**49** (6), `base_conhecimento`=5 (AEC intocada), 6 `is_curated=true` professional-scoped, 0 visível a paciente, links HTTP 206. Tags `fase_a_06_06`/`fase_a_piloto_06_06` (reversível). **content=NULL** → Fase A.2 (OCR via extract-document-text) pendente. Lock V1.9.318 respeitado (não tocou base_conhecimento).

**Lição:** "index 103" do checklist → empírico → **6 reais**. Dedup-vs-catálogo + triagem evitou duplicatas + vazamento LGPD. [[project_matrix_z2_fase_a_curadoria_103_orfaos_06_06]].

## BLOCO G — Fase A.2 EXECUTADA (OCR via extract-document-text)
Edge `extract-document-text` (pdfjs-serverless, service_role, aceita `document_id`, re-assina URL, UPDATE content). Pilot no menor (Mycophenolate 136KB → 8.768 chars) → escala 5 restantes. **Todos os 6 com sucesso, incluindo os grandes** (Finerenone 4.9MB→54k chars; Towards-AI 9.4MB→120k chars). Resultado: 6/6 com content (média **50k chars**), `base_conhecimento`=5 (AEC intocada), documents=49. **Fase A COMPLETA**: corpus científico do Ricardo na Curadoria, full-text searchable, professional-scoped, zero regressão. Reversível por tag. Pendente decisão Ricardo: pessoais (CV/Dossiê/Sopro) → Meus Documentos ou fora.

## BLOCO H — Fila sem-regressão (manhã 06/06)
- **#1 wallets RLS admin-read (V1.9.607, `0104ebf`):** `wallets` só tinha `owner_select` → admin sub-contava (grounding Nôa "1 carteira" vs 4). Fix: policy SELECT `is_admin()` aditiva (OR). Verificado RLS real: admin vê **4** (era 1), usuário comum inalterado. Zero regressão.
- **#3 doc institucional `docs/CAMADA_COGNITIVA_CAR_DIGITAL.md`:** Matrix Z2 + Sidecar Renal + AEC/MIMRE + Constituição = segurança do paciente via gestão de risco semântica pré-doença (3ª vertente Ricardo). Auditor-safe, anti-overclaim (não diagnostica / não SaMD-decisional / remissão DRC investiga-não-afirma). Sem dados de paciente.
- **#2 padrão visual: NÃO tocado às cegas.** Empírico: AlunoDashboard já tem `style={{background: backgroundGradient}}` (gradiente inline) e TeamManagement já usa translucent emerald + é sub-componente. "Padronizar pro `.app-bg-gradient`" = trocar gradiente funcional por outro = mudança visual subjetiva → respeitar regra [[feedback_landing_visual_iteracao_screenshot_loop_02_06]] (não chutar visual; Pedro itera no olho). Gap cosmético, não quebra.

## BLOCO I — Visual: botões verde sólido → translucent emerald (V1.9.608, `6dba136`)
Pedro **na tela** (Relatórios Consolidados, /app/reports) apontou botões com gradiente verde sólido `#00C16A` + texto branco que destoavam do padrão. Trocados pelo translucent emerald cristalizado: **"Abrir para revisar"** (o mais berrante, em todo card), **"Atualizar"**, aba ativa **"Todos"** → `bg-emerald-500/15` + `border-emerald-500/30` + `text-emerald-300` + `hover:scale-[1.02]`. Removidos `style` inline + handlers `onMouseEnter/Leave` (Tailwind puro). type-check 0 + build OK. **Princípio respeitado:** visual SÓ com Pedro vendo (ele apontou + Vercel deploya pra ele conferir) — não às cegas [[feedback_landing_visual_iteracao_screenshot_loop_02_06]]. Pendente sweep do `#00C16A` em outras telas + decisão sidebar nav. [[reference_padrao_visual_app_translucent_emerald_max_w_screen_2xl_31_05]].

## BLOCO J — Sidecar Renal ELITE (V1.9.609) + mapa de evidência do corpus
Pedro recordou as 2 oportunidades da reunião (captação renal precoce + CBD/CBG) e perguntou como somam aos 2 cards (Sidecars Cognitivos: Renal + Neuro) de forma elite/coerente. Resposta: **enriquecer o `RenalSuggestionsCard`, não card novo.**

**Construído (wrapper, card real INTOCADO — zero regressão por construção):** `RenalSuggestionsCardElite.tsx` com 3 camadas: ① Sinais Precoces (cálculo/IRA/dor lombar/disúria — scaffold, parser aguarda GO Ricardo+slug-test) · ② estagiamento existente byte-idêntico · ③ Conduta & Evidência (auditor-safe). Gate `?renal_elite=1` (live default = card normal). Padrão translucent emerald. type-check 0 + build OK.

**🔬 Mapa de evidência do corpus (empírico, lendo o content OCR dos papers indexados hoje):**
| Evidência | Nº docs | Força |
|---|---|---|
| CBD + symptom management (CKD) | **5** | 🟢 forte |
| CB1 receptor + fibrose | 2 | 🟢 mecanístico |
| Pruritus urêmico | 2 | 🟢 concreto |
| **CBG/cannabigerol** | **1** | 🔴 **tênue → hipótese** |

Trecho real Ho et al 2019: *"treatment of common symptoms... in advanced stages of CKD, including chronic pain, nausea and vomiting, anorexia, pruritus, and insomnia"* (revisão sistemática MEDLINE/EMBASE). **Conclusão sem-riscos:** a visão é bem embasada pra **CBD-sintomas + mecanismo CB1**; **CBG é fronteira de pesquisa** (1 menção) → mantém "investigação", nunca recomendação. App já tem PubMed ao vivo (useExternalLiterature) pra estudos novos. [[project_evidencia_corpus_cannabis_renal_06_06]].

## BLOCO K — Liga o pipeline do Sidecar Renal (V1.9.610) — investigação que evitou fix errado
Pedro perguntou por que a Gisele não tem card + como as AECs auto-populam. Investigação empírica profunda:
- **Gisele = caso NEURO** (queixa "crises convulsivas") sem dado renal → card vazio CORRETO. Neuro não auto-detecta (neuro-signal-extractor não existe, Fase D pendente Eduardo).
- **Pipeline renal NÃO auto-populava:** captation_extra=**1 row** em 3.509 chat_turns. 
- **🚨 Hipótese errada evitada:** pensei em scan no chat_turn cru — mas o `payload.input` é **poluído com contexto RAG** (creatinina vem dos PAPERS injetados, não do paciente). Scan cru criaria sugestões FALSAS. O modelo empírico (ver o input real) salvou.
- **Verdade:** o Core (V1.9.303, linha 6260) JÁ classifica certo via GPT-4o-mini **só a fala do paciente** em `laboratorios_inline` — mas é gated (>300 chars + fase elegível = ~10% turns), raro pré-PMF. A cadeia FUNCIONA (Maria das Dores provou, Ricardo aprovou 17/05). Faltava **1 peça: o cron** (o extrator diz "pg_cron futuro").

**Fix V1.9.610:** cron `renal-signal-extractor-15min` (`*/15`, padrão de video-call-reminders). Invocação manual pré-cron: success/0 erros/idempotente. **Zero Core, zero regressão** (extrator decoplado/read-only/idempotente/flag-gated). Agora AEC com labs → captation_extra → cron ≤15min → sugestão → card. **Expansão pra sintomas precoces (Opp A) segue separada/pendente Ricardo.** [[project_evidencia_corpus_cannabis_renal_06_06]].

## 🚀 Frase âncora 06/06
> *"O checklist dizia 'indexar 103 órfãos, risco baixo'. O modelo empírico (checar os porquês antes) revelou: são só ~17 papers reais — o resto é duplicata, DOCX que o OCR não lê, doc interno, projeto Dengue, e — crítico — um pedido de exame de PACIENTE (LGPD) + CV/IP pessoais do Ricardo que NÃO devem subir. 'Indexar tudo' teria poluído o acervo e vazado dado de paciente. Boundary cravado: documents (acervo app) ≠ base_conhecimento (RAG clínico, AEC intocada) ≠ embeddings (off). Curadoria entregue pro Ricardo aprovar; indexação fica a 1 GO de distância, professional-scoped, zero regressão. Triple-A: investigar → triar → validar → executar."*
