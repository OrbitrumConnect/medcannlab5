# Diário de bordo — 3 de abril de 2026 *(plano operacional)*

**Tipo:** dia seguinte à auditoria manual + relatório Lovable — **validar, ligar com segurança, não regredir**  
**Documento mestre associado:** `docs/TIMELINE_LIVRO_MAGNO_DIARIOS_CONSOLIDADO.md` · `DIARIO_02_04_2026.md` §7–8

---

## 1. Princípio do dia

**Uma alavanca de cada vez**, com evidência antes e depois. Proibido: “ativar 15 módulos”, `DROP`, migração massiva legacy → novo sem backup e critério clínico.

---

## 2. Ordem recomendada *(manhã → tarde)*

| Fase | O quê | Como não quebrar |
|------|--------|------------------|
| **A — Congelar e medir** | Anotar branch/SHA deploy; no Supabase, **3 contagens** que o Lovable citou: `chat_messages`, `chat_messages_legacy`, `patient_medical_records` (chat), `ai_chat_interactions` | Só `SELECT`; mesmo projeto que o app usa |
| **B — Verificar `chat_messages` = 0** | Um teste manual: fluxo que usa `useChatSystem` ou `PatientChat` → enviar **uma** mensagem de teste; ver se insert retorna erro (Network tab / console) | Conta de teste, sala de teste; se RLS bloquear, **corrigir policy ou usar RPC** — não desligar RLS globalmente |
| **C — Rastrear IMRE “vazio”** | `grep` / código: onde `imre_assessments` é escrito vs onde o fluxo AEC grava (`clinical_reports`, `patient_medical_records`) | Se o produto **já** cumpre o relatório clínico noutro sítio, documentar “fonte de verdade” no Magno — **não** duplicar escrita sem desenho |
| **D — Uma view ou um KPI (opcional)** | Se B/C estiverem esclarecidos: escolher **uma** view já existente (ex. `v_kpi_basic` ou `v_doctor_dashboard_kpis`) e **uma** tela admin/dashboard que hoje faz N queries — substituir por **uma** chamada | Feature flag ou branch; rollback = reverter commit único |
| **E — Curadoria documentos (só se houver tempo)** | Revisar PREVIEW de `FIX_DOCUMENTS_UNLINK_DEMO_TITLES_FROM_AI.sql`; executar **só** se a lista for 100% aceitável | `BEGIN`/`COMMIT`; não misturar com B/C no mesmo bloco emocional |

---

## 3. O que **não** fazer amanhã *(reduz risco)*

- Migrar `chat_messages_legacy` → `chat_messages` em lote sem mapear `room_id` / remetentes.
- Popular `imre_assessments` à mão só para “não estar zero” sem contrato com o Core.
- Alterar políticas RLS em **137 tabelas** ou `USING (true)` “temporário”.
- Apagar `usuarios`, `pacientes`, `interacoes_ia` (legado) sem inventário jurídico/retention.

---

## 4. Critério de “dia bom”

- [ ] Ficou **escrito** (neste diário ou comentário Git) **onde** o chat P2P grava e por que `chat_messages` pode estar 0.  
- [ ] Ficou **escrito** **onde** o resultado IMRE/AEC vive hoje (tabelas + ficheiros).  
- [ ] Zero regressão em: login paciente, AEC até relatório, agendamento RPC, um chat de teste.

---

## 5. Encaixe pitch / Magno

- O relatório Lovable suporta narrativa de **infraestrutura e RLS**; as decisões de amanhã devem **reforçar uma fonte de verdade por domínio** (chat clínico vs chat P2P vs legacy), alinhado ao **Clinical OS** — mesmo texto que §7–8 do diário 02/04.

---

*Plano revisável ao fim do dia 03/04; marcar o que foi feito com hora e evidência (print/query).*
