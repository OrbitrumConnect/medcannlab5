# DIÁRIO 02/06/2026 — Sessão laptop pós-pull: housekeeping + Gisele + V1.9.560 Library separação Curadoria/Meus

**Sessão**: laptop, tarde→noite 02/06.
**Estado de entrada**: HEAD `8ebc61d` (V1.9.557, sincronizado via pull desktop com 50+ commits da maratona 30→01/06).
**Estado de saída**: HEAD `85bef6d` (V1.9.560-C) + 5 commits cirúrgicos no dia + validação visual cross-account com Ricardo ✅.

---

## 🎯 OBJETIVO DESTA SESSÃO

Pós-pull do desktop (que executou 100% do plano DIARIO_30 + extensão massiva), Pedro pediu sequência de polimento e ajustes cirúrgicos "sem regressão". Durante o trabalho, dois achados serendipitosos emergiram:

1. **Drift Motor/Método na ref bio Ricardo 31/05** (PC desktop travou durante criação da memória)
2. **Gap arquitetural Library**: docs subidos por profissional caem na mesma tabela institucional, visíveis a todos os outros profissionais e alunos — violando expectativa "doc do profissional é dele"

Ambos viraram entregas cirúrgicas no mesmo dia, com smoke matriz empírico.

---

## 🧹 BLOCO A — V1.9.558 Housekeeping pós-DIARIO_30

Commit `e53932b`. Push 4/4 refs OK.

### A.1 — `git clean -fdX tmp/` (subdirs gitignored)
- 321 audit JSON acumulados → removidos (323 → 2 subdirs não-ignored)
- `.gitignore:29` já tem `tmp/` desde V1.9.504 ✅
- Subdirs `cert_smoke/`, `muhdo/`, `fix_button.py`, `supabase_types.ts` mantidos (não casam pattern)

### A.2 — 4 diffs cirúrgicos em CLAUDE.md
| # | Linha | Antes | Depois |
|---|---|---|---|
| 1 | 9 (HEAD) | `cae44e9` (31/05) | `8ebc61d` (01/06 V1.9.503→557 sintese pull) |
| 2 | 95 (npm build) | "⚠️ quebra local por dompurify ausente" | "vite build (dompurify ^3.4.0 instalado)" |
| 3 | 310 (cron monthly) | "às 3h" + "01/06/2026 03h BRT" | "00:00 BRT (03:00 UTC pg_cron) — 1ª exec falhou 01/06 null user_id, ZERO poluição, Pedro decidiu DORMENTE até lançamento" |
| 4 | 417 (cfm) | "32 rows" | "52 rows" |
| 5 | 419 (dompurify duplicado) | claim de erro | strikethrough + RESOLVIDO 01/06 |

Decisão Pedro: 4 deltas conhecidos, escopo literal, sem expandir.

---

## 🔧 BLOCO B — V1.9.559 Fix drift "Método" → "Motor" Incentivador

Commit `77b06b3`. Push 4/4 refs OK.

### B.1 — Detecção pela intuição empírica do Pedro

Pedro: *"esse documento do dia 31 pode estar errado entao esse nome pois se so aparece aqui algo contaminou em algum momento ontem meu pc travou mesmo!"*

Validação empírica (triangulação 3 fontes históricas vs 1 isolada):

| Fonte | Data | Termo |
|---|---|---|
| `project_mimre_pesos_narrativos_24_04` | 24/04 | **Motor** Incentivador |
| `docs/MENSAGEM_PAULO_CNPJ_BRIEFING_05_05_2026.md:167` | 05/05 | **Motor** Incentivador |
| `docs/memorias/MEMORY.md:313` | índice | **Motor** Incentivador |
| `reference_ricardo_valenca_bio_autoral_mimre_31_05.md` | criada 01/06 18:52 BRT desktop | **Método** Incentivador |

Conclusão: **3 fontes históricas independentes vs 1 fonte criada pós-crash** = drift editorial isolado. Corrigido 3 diffs cirúrgicos (frontmatter description + header section + nota final auditável).

### B.2 — Erro Claude cometido no caminho: encoding UTF-8 Windows cURL `-d` inline

Apliquei UPDATE Gisele `allergies = 'Rinite alérgica'` via curl `-d` inline → shell Windows converteu encoding errado → banco recebeu `Rinite al�rgica`. Detectei via byte-level (`octet_length` vs `length`). Corrigi com `chr(233)` (técnica shell-independent).

**Anti-padrão AP-1 do catálogo** (*"leia memória antes de agir"*) em ação ao vivo: memória `feedback_nunca_criar_auth_users_sql_direto_tokens_null_31_05` cristalizou essa lição há 1 dia, eu havia lido na rodada de leitura profunda 2-3h antes, e ainda caí no mesmo padrão. Princípio meta cravado pra mim: **toda escrita PT-BR via PAT exige PowerShell `--data-binary @file` OR `chr(N)` inline**.

---

## 🩺 BLOCO C — Auditoria empírica paciente Gisele (paciente externa real do dia)

Pedro pediu avaliação completa: o que ela fez, com quem marcou consulta, etc.

### C.1 — Identidade
- **Gisele Ribeiro Gonçalves** | UUID `6fb08246` | Email `giselergoncalves@hotmail.com`
- Type: paciente | Payment: paid | Status: active
- Cadastrou 01/06 11:49 BRT, completou onboarding em 60 segundos, AEC 49 min, relatório assinado ICP

### C.2 — Cronologia 01/06 (BRT)

| Horário | Evento |
|---|---|
| 11:49:22 | Cadastro `public.users` |
| 11:49:55 | Consent aceito |
| 11:50:23 | Onboarding completo |
| **12:02:48** | **🎯 Marcou consulta** direto via widget → Dr. Ricardo Valença, 13:00 BRT, R$ 400, remoto |
| 12:06 | Primeira mensagem chat ("eu vou botar 12:30 ou 13:30") — tentou REMARCAR |
| 12:08 | Clicou "Revisão de consulta" → **AEC GATE V1.5 disparou** bloqueando até fazer AEC |
| 12:15 | Trigger "Iniciar Avaliação Clínica Inicial IMRE Triaxial" |
| 12:28 | AEC formal iniciada (terceiro estava com ela ajudando — transcrição revelou) |
| 13:00 | ⏰ Horário marcado da consulta (paciente ainda na AEC) |
| **13:17:18** | ✅ Relatório COMPLETED + signature_hash ICP gerado |
| 13:17:30 | Racionalidade `integrative` (noa_ai automática) |
| 13:17:42 | "Sim" → consentimento registrado |
| 14:46:39 | Último acesso conhecido |

### C.3 — Conteúdo clínico
- **Queixa principal**: epilepsia desde os 18 anos (crises convulsivas)
- **Medicações**: Lamotrigina 100mg + Oxcarbazepina 300mg + Urbanil 10mg + Óleo CBD (~5 anos)
- **Alergias**: rinite alérgica
- **Familiar**: nada grave na mãe; avó faleceu 96 anos câncer de mama (paterno)

### C.4 — Achados estratégicos

1. **Gisele é o caso mais próximo de "paciente externa real" que o sistema já produziu** (não é teste interno, payment_status=paid, persona MedCannLab core)
2. **Bug V1.9.546 consent_given fechou empíricamente nela**: primeiro paciente externo pós-fix gravou `consent_given=true` corretamente
3. **V1.9.534 DROP trigger auto-TRIAGE legacy funcionou**: ZERO rows em `clinical_assessments` pra ela
4. **AEC FSM rodou 14 fases sem trava em 49 minutos**

### C.5 — Pergunta arquitetural Pedro: ela queria Ricardo OU Eduardo?

Investigação empírica:
- Epilepsia = especialidade Eduardo (Neuropediatria + Neurofisiologia Clínica), NÃO Ricardo (Nefrologia)
- Gisele NÃO é paciente prévia de Eduardo (zero matches no histórico dele)
- ZERO menções a "Eduardo"/"Faveret"/"neuro" no chat dela
- Marcou via widget direto sem mediação Nôa
- Terceira pessoa estava ajudando ela ("elevado eu estou aqui com a Gisele que tá fazendo avaliação")

**Veredito**: provavelmente confusão da paciente — vitrine não direciona pra especialidade adequada baseado em queixa. Mas amostra = 1, sem trigger empírico pra mudar UI (anti-Babylon).

---

## 🔧 BLOCO D — Fix prontuário Gisele (allergies + medications + lição encoding)

Pedro flagrou via screenshot: prontuário Carolina tem `allergies="Dipirona, poeira"` e `medications="Vitamina D 2000UI/dia"`, mas Gisele tem ambos NULL.

### D.1 — Causa raiz arquitetural
- Dado existe em `clinical_reports.content.perguntas_objetivas` (assinado ICP)
- Frontend lê de `users.allergies` / `users.medications` (campos texto)
- **NÃO existe trigger ou RPC que extrai do report e popula users**
- Carolina tem populado porque foi cadastrada em onboarding antigo com formulário rico; Gisele completou onboarding em 60s sem preencher nada

### D.2 — Fix pontual (decisão Pedro: não esperar outros pra cristalizar)
UPDATE pontual respeitando fonte canônica:
```sql
UPDATE users SET allergies='Rinite alérgica', medications='Lamotrigina 100mg, Oxcarbazepina 300mg' WHERE id = '6fb08246...';
```

### D.3 — Encoding drift detectado e corrigido (lição V1.9.559 não aplicada)
Primeira tentativa via curl `-d` inline → banco recebeu `Rinite al�rgica` (encoding quebrado). Correção via `chr(233)` (técnica shell-independent UTF-8). Byte-level validation: 16 bytes / 15 chars confirmando `é` multibyte UTF-8 correto.

---

## 🗂 BLOCO E — Pergunta arquitetural Pedro sobre Library / base de conhecimento

Pedro: *"temos a nossa base de conhecimento! aonde adm sobe docs e temos uma base de conhecimento do proficional que sobe os docs dele etc! agora aonde fica isso em que area etc"*

### E.1 — Mapa empírico das 3 fontes
| Tabela | Rows | Propósito | UI |
|---|---|---|---|
| `base_conhecimento` | 5 | RAG hand-crafted Ricardo | sem UI (proteção V1.9.318) |
| `documents` | 43 | Acervo institucional compartilhado | `/app/library` |
| `patient_documents` | 2 | Docs por paciente (RLS) | aba Arquivos prontuário |

### E.2 — Achado crítico
Pedro intuiu o gap: *"os documentos q ele sobe nao e para a base de conhecimmento e sim para outro local porem aparece tudo na aba base de conhecimento para ele?! duvida..."*

Investigação:
- Upload do profissional via Library cai em `documents` (mesma tabela institucional)
- Policy RLS `docs_select_authenticated` tinha `OR has_role('profissional')` → todo profissional vê TUDO
- 42/43 docs com `uploaded_by=NULL` (campo nunca populado pelo frontend)
- `author` é texto livre (drift "Dr." vs sem prefixo)

### E.3 — Decisão arquitetural Pedro
*"acho que o correto seria entao separar os docs subidos por usuarios dos docs do app ne?"* → confirmou. Adicionalmente: *"problema que documento do proficional e dele nao seria correto outros proficinals verem! nem alunos pois ele pode subir doc pessoal dele! ja temos o forium para compartilhar casos ne melhor"* → privacidade default + Fórum (V1.9.403-410) já resolve compartilhamento curado.

---

## 🏗 BLOCO F — V1.9.560 Cenário 3 defensivo (3 fases A+B+C)

### F.1 — V1.9.560-A Backend prep (commit `a523080`)

Migration `20260602000000_v1_9_560_documents_uploaded_by_backfill.sql`:

1. **Backfill `uploaded_by` por author** (rastreabilidade):
   - 30 docs `Dr.|Ricardo Valença` → UUID `2135f0c0...`
   - 5 docs `Dr. Eduardo Faveret` → UUID `f4a62265...`
   - 7 sem author ficam órfãos (institucionais puros)
   - 1 doc preserva uploaded_by histórico (`3d6b170c...`)

2. **Backfill `is_curated=true` defensivo**: 41/43 → 43/43 (preserva acesso atual)

3. **Trigger BEFORE INSERT** `documents_set_uploaded_by`: garante `uploaded_by = auth.uid()` em todos os INSERTs futuros (frontend não precisa setar manualmente)

Pré-migration: `uploaded_by NULL=42`, `is_curated=true: 41`.
Pós-migration: `uploaded_by NULL=7`, `is_curated=true: 43`, trigger ✅.

### F.2 — V1.9.560-B Library 2 abas + checkbox (commit `b4c47df`)

Library.tsx (+72 linhas, 8 edits cirúrgicos):
1. State `activeViewTab: 'curadoria' | 'meus'` (default 'curadoria')
2. State `shareWithPlatform: boolean` (default false, privado)
3. UI 2 abas antes do bloco busca (BookOpen + FileText)
4. Filtro `matchesViewTab` em ambas funções (busca normal + semântica):
   - `'curadoria'`: `is_curated !== false`
   - `'meus'`: `uploaded_by === user?.id`
5. useEffect deps + `activeViewTab` + `user`
6. Upload modal: checkbox "Compartilhar com a Curadoria da Plataforma" (default OFF)
7. `documentMetadata` ganha `is_curated: shareWithPlatform`
8. Reset `shareWithPlatform=false` pós-upload

Type-check verde. Zero impacto runtime backend ainda (RLS não tocada nesta fase).

### F.3 — V1.9.560-C RLS tighten Variante 1 (commit `85bef6d`)

Migration `20260603000000_v1_9_560_C_rls_tighten.sql`:

```sql
-- ANTES
USING ((uploaded_by = auth.uid())
       OR has_role('admin')
       OR has_role('profissional')   -- TODO prof via TUDO (gap raiz)
       OR (is_published = true))

-- DEPOIS (Variante 1, preserva paciente)
USING ((uploaded_by = auth.uid())     -- owner sempre
       OR has_role('admin')
       OR (is_curated = true AND
           (has_role('profissional')
            OR has_role('aluno')))    -- prof/aluno só veem curadoria
       OR (is_published = true))      -- paciente igual antes
```

**Variante 1 escolhida pela preservação do paciente** (40 publicados continuam acessíveis, sem regressão).

### F.4 — Smoke matriz 9/9 PASS (empírico via PAT simulando policy)

| # | Smoke | Esperado | Real | Status |
|---|---|---|---|---|
| 1 | Paciente vê (só is_published) | 40 | 40 | ✅ zero regressão |
| 2 | Ricardo prof vê (owner 30 + curadoria) | 43 | 43 | ✅ |
| 3 | Eduardo prof vê (owner 5 + curadoria) | 43 | 43 | ✅ |
| 4 | Aluno vê (curadoria + publicados) | 43 | 43 | ✅ |
| 5 | INSERT doc privado Ricardo (is_curated=false) | OK | OK | ✅ trigger uploaded_by funcionou |
| 6 | Ricardo (owner) vê próprio privado | 1 | 1 | ✅ |
| 7 | Eduardo (não-owner) NÃO vê privado | 0 | 0 | 🎯 **GAP FECHADO** |
| 8 | Aluno NÃO vê privado | 0 | 0 | 🎯 |
| 9 | Paciente NÃO vê privado | 0 | 0 | 🎯 |

CLEANUP: doc teste deletado.

### F.5 — Validação visual cross-account com Ricardo ✅

Pedro foi pessoalmente ao Ricardo, mostrou a UI:
- ✅ 2 abas visíveis (Curadoria + Meus Documentos)
- ✅ Ricardo vê docs dele + docs do app
- ✅ Comportamento esperado confirmado empíricamente

Aplicação direta do princípio meta cristalizado 28/05: *"validação empírica via screenshot > plano teórico"*.

---

## 🎓 BLOCO G — Princípios meta cristalizados/reforçados HOJE

### G.1 — Pattern UTF-8 Windows: PowerShell + curl `--data-binary @file`
**Cristalizado novo**. Trigger: V1.9.559 caí no mesmo anti-padrão que memória 31/05 documentou (curl `-d` inline com PT-BR quebra encoding). V1.9.560-A e V1.9.560-C aplicaram a solução correta:
1. PowerShell escreve JSON UTF-8 sem BOM (`System.Text.UTF8Encoding($false)`)
2. curl `--data-binary @file.json` + `-H 'Content-Type: application/json; charset=utf-8'`
3. Funcionou: `Valença` (ç) gravado byte-level correto (16b/15c)

### G.2 — Fase A→B→C atomic com smoke matriz por papel (RLS Camada 0)
**Cristalizado novo**. Pattern reusável pra próximos refators de RLS:
- **Fase A** (backend prep): backfill + trigger SEM mudar policy → zero impacto runtime
- **Fase B** (frontend): UI já funcional mesmo sem RLS tightening → permite teste visual antecipado
- **Fase C** (RLS tightening): mudança Camada 0 com smoke matriz por papel ANTES de pushar
- **Smoke 9/9**: cada papel × situação (positiva + negativa) + INSERT/DELETE teste limpo

### G.3 — Triangulação 3 fontes históricas vs 1 fonte isolada = drift detectado
**Cristalizado novo (V1.9.559)**. Quando fonte nova contradiz N fontes históricas independentes, descartar a fonte nova. Validação cross-source > confiança em fonte recente isolada.

### G.4 — "Pedro intui empíricamente" reforçado (caso Library)
Pedro intuiu: *"problema que documento do profissional e dele nao seria correto outros proficinals verem"*. Validação empírica via RLS audit confirmou: policy permissiva `OR has_role('profissional')` = todo prof via TUDO. **Intuição empírica do Pedro é sensor, não opinião** (princípio cristalizado 30/05). Aplicado HOJE 3+ vezes (drift Motor/Método, gap Library, decisão fix Gisele sem esperar outros).

### G.5 — "Polir não inventar" reforçado
Library V1.9.560 não criou tabela nova `professional_personal_documents`. Reusou:
- `documents.is_curated` (já existia no schema, nunca usado)
- `documents.uploaded_by` (existia, NULL em 42/43)
- Trigger BEFORE INSERT (padrão Postgres canônico)
- 2 abas frontend (reusou estrutura de filtros existente)

### G.6 — Princípio escopo literal Pedro
Não expandi pra: hard-delete 5 Edges em observação, copy "Iniciar sua Entrevista", bug AEC-como-profissional Ricardo, refator tradevision-core, proteção IMRE legado Lovable (Pedro pediu pra parquear: *"problema que documento do proficional e dele... acho que por hora esperamos"*). Cada um vira frente futura quando Pedro priorizar.

---

## 📋 BLOCO H — Pendências e próximas frentes

### Frentes parqueadas (do plano polimento original)
```
⏸ ITEM 4: copy "Iniciar sua Entrevista" (Pedro: "por hora esperamos")
⏸ ITEM 5: hard-delete 5 Edges em observação (get_chat_history + google-auth +
   sync-gcal + renal-signal-extractor + video-call-request-notification)
⏸ ITEM 6: audit empírico bug AEC-como-profissional Ricardo flagou (CORE)
```

### Frentes novas descobertas hoje
```
🟡 UI edição doc Library: hoje só novos uploads respeitam checkbox. Pra Ricardo
   tirar algum dos 30 docs históricos da Curadoria (deixar privado dele),
   precisa de UI nova "editar visibilidade do doc". Próxima feature parqueada
   até demanda real Ricardo/Eduardo flaggar.

🟡 Documents.author drift Dr./sem prefixo: 24 "Dr. Ricardo" + 7 "Ricardo
   Valença" — 2 entries pro mesmo autor. Cosmético, não bloqueia.

🟡 Vitrine profissionais não direciona paciente pra especialidade adequada:
   Gisele (epilepsia) marcou com Ricardo (nefro) quando Eduardo (neuro) seria
   match. Amostra = 1, anti-Babylon: parqueado até 2-3 casos similares.

🟡 Trigger pg extraindo perguntas_objetivas → users.allergies/medications:
   Hoje só foi UPDATE manual da Gisele. Próximo paciente externo expõe o gap
   de novo. Considerar trigger global em fase futura.
```

### Marcos humanos (destrava roadmap)
```
🔴 CNPJ João Vidal (Marco 1) — destrava Stripe Connect
🔴 Manual v1.1 + uso regular Eduardo (Marco 3)
🔴 20-30 pacientes externos pagantes (PMF declarável Marco 2)
🔴 WiseCare homolog → produção (contrato V4H)
```

---

## 📌 BLOCO I — Commits da sessão (5 entregas + diário)

| Commit | Versão | Push 4 refs |
|---|---|---|
| `e53932b` | V1.9.558 housekeeping pós-DIARIO_30 | ✅ |
| `77b06b3` | V1.9.559 fix drift "Método"→"Motor" Incentivador | ✅ |
| `a523080` | V1.9.560-A backend prep (backfill+trigger) | ✅ |
| `b4c47df` | V1.9.560-B Library 2 abas + checkbox | ✅ |
| `85bef6d` | V1.9.560-C RLS tighten Variante 1 | ✅ |
| (este commit) | V1.9.560-D DIARIO_02_06 + 2 memórias | pendente |

**Locks intocados em TODA a sessão**: V1.9.95, V1.9.97-D, V1.9.98, V1.9.99-B, V1.9.299 (PBAD ICP), V1.9.388-A.3, V1.9.452 (PII), V1.9.468-B (Matrix Z2 + Bula).

---

## 🚀 BLOCO J — Frase âncora 02/06

> *"Sessão laptop pós-pull: 5 entregas cirúrgicas sem regressão. V1.9.558 limpou casa (CLAUDE.md + tmp). V1.9.559 detectou drift editorial isolado (Método→Motor) via triangulação 3 fontes históricas vs 1 nova. V1.9.560 (A+B+C) executou Cenário 3 defensivo: docs do profissional ficam privados por default, RLS Variante 1 preserva paciente, smoke matriz 9/9 PASS, validação cross-account com Ricardo OK. Pattern Fase A→B→C atomic com smoke matriz por papel cristalizado pra próximos RLS tightenings. Pattern PowerShell UTF-8 + curl --data-binary @file cristalizado como solução canônica pra escrita PT-BR via PAT (lição V1.9.559 aplicada V1.9.560 com sucesso). Intuição empírica do Pedro continua sendo sensor, não opinião (drift Motor/Método + gap Library detectados via 'duvida?' do Pedro)."*

---

**Estado de saída**: HEAD esperado pós-commit V1.9.560-D (este diário + 2 memórias). Próxima sessão pode atacar uma das frentes parqueadas OU aguardar empírico de uso real (Ricardo/Eduardo subindo docs privados, Marco 2 paciente externo pagante).

---
---

# 🖥️ PARTE 2 — SESSÃO DESKTOP (02/06, tarde→noite) — Regulatório/FHIR + Landing visual

> Continuação no desktop após o pull do laptop (V1.9.558-560). Tudo abaixo NÃO está em outros diários. Foco: (a) reunião Donizete/ANVISA → deliverable FHIR auditor-safe, (b) iteração visual da landing [PREVIEW], (c) roadmap das 5 frentes técnicas.

## BLOCO K — Landing visual (experimento Gptest) ⚠️ NÃO COMMITADO
Iteração ao vivo (HMR :3000) — `src/pages/Landing.tsx` (M) + `public/gptest-logo.png` (untracked):
- Banner **Gptest** no hero (oval: `rounded-full` + `aspect-[11/10]` + `object-cover scale-125`, ~30% crop, `max-w-[18.8rem]`)
- Anéis **água-ripple ciano** saindo da borda do oval (chips de circuito testados e descartados)
- BG: testamos alternância escuro↔degradê → **unificado no gradiente canônico do app** (`.app-bg-gradient` #0f172a→#1e293b→#1e3a3a)
- Texto verde→ciano→**rebalanceado pra TEAL** (ficou ciano demais) + verde-puro `green-500`→teal; botão "Criar Conta" → **ghost translúcido** (`bg-cyan-500/10`+border+backdrop-blur)
- **DECISÃO PENDENTE**: commitar (V1.9.561) OU reverter. (Gptest = experimento de logo; paleta teal/ghost parece keeper. Pedro decide.)

## BLOCO L — ⭐ Deliverable regulatório: doc HL7 FHIR R4 (COMMITADO)
Reunião com **Donizete** (ex-dono farmácia/distribuidora + contatos ANVISA/ministérios, **saturado de healthtech** → abordagem anti-hype + substância). HL7 citado → mapeamos interop.
- Criado **`docs/INTEROP_FHIR_MEDCANNLAB_R4.md`** (commit `4194bd5` + §9 roadmap `bd490c1`): **21 recursos FHIR R4** mapeados campo-a-campo, grounded no schema real via PAT.
- **2 workflows multi-agente** (DB+código+regulatório+auditor) pegaram **2 OVERCLAIMS CRÍTICOS**:
  1. `clinical_reports` tem **hash SHA-256 de integridade**, NÃO assinatura ICP (PKCS#7 real só em `cfm_prescriptions`/`patient_exam_requests`; populado 47/150). Assinar a `Composition` = roadmap.
  2. **"não-SaMD" → "SaMD Classe IIa"** (baseline do próprio SGQ/EXECUTIVE_SUMMARY); não-decisional = **mitigação de classe**, não isenção.
- **2 reviews externos** (GPT auditor 9.0→9.5) → linguagem auditor-safe: Observation "potencialmente exportável", LOINC ilustrativo, Consent "candidato natural a Consent+Provenance", RAC "aderência estrutural", terminologia = maior esforço, "IA influencia contexto (sujeita a avaliação formal)".
- **Re-confirmação empírica AO VIVO (PAT)**: TODOS os números batem — clinical_reports 150/47, cfm 52, exames 25, rat 141, scores 625, axes 530, nft 37, sigconf/pki 12, appt 96, vcs 134, aec 18 (consent 10), **RLS 145/145**, roles pac50/prof15/admin5/aluno1, content sections (ident/lista 114, rat 97, scores 93, rec 21), vazias conditions/therap/lab = 0/0/0.

## BLOCO M — Enquadramento regulatório consolidado
Diretrizes que se aplicam + status: **ISO 13485/IEC 62304/ISO 14971** (16 drafts SGQ 🟡) · **RDC 657/751** (SaMD Classe IIa, petição Marco 4 🔴) · **SBIS-CFM S-RES** (alinhado 🟡) · **CFM 2.314/2022** (conforme 🟢; **2.454/2026 a validar** — citação sem lastro) · **LGPD** (RLS 145/145 🟢, pseudonim ~86% 🟡) · **ICP-Brasil** (CONFORME ITI em prescrições/exames 🟢) · **FHIR/RNDS** (arquitetura compatível 🟡; faltam serializer + terminologia + homologação).
"Como saber se estamos de acordo" = gap-analysis formal da **consultora SaMD (Fase 1)** contra checklist ANVISA. "100% conforme" = submission-ready interno + carimbo externo (consultora+RT+ANVISA+certificadora).

## BLOCO N — Slides Donizete (PDF)
img2pdf montou PDF inicial → Pedro fez **15 slides Genspark** (deck FHIR) → review: **14/15 auditor-safe**, slide 15 reintroduziu "FHIR/RNDS Ready" (overclaim) → **removido via pypdf** (PDF final **14p**, backup 15p guardado). `MedCannLab - Donizete.pdf` no Desktop.

## BLOCO O — Roadmap das 5 frentes (PRÓXIMO: atacar)
2 trilhas pra "100%": **técnica (atacável já, sem CNPJ)** + **humana (Marco 1 CNPJ = gargalo-mãe)**.
Ordem técnica por ROI×risco:
1. **Serializer FHIR PoC** (clinical_report → Composition+Bundle) — maior ROI, **risco zero** (read-only, não toca lock), torna "compatível" em "exporta de verdade" ← **ATACAR PRIMEIRO**
2. Gaps operacionais SGQ (pre-push hooks, cadência QA) — baixo/zero
3. ICP-Brasil no relatório clínico — 🔴 mexe lock V1.9.299 → sessão dedicada smoke ITI
4. Binding terminologia (CID-10/LOINC) — alto, precisa Ricardo
5. Conformidade br-core — depende do #1

## Conceito refinado (não-overclaim)
**AEC = entrevista/escuta estruturada, NÃO "a IA avalia o paciente"** — IA organiza, médico avalia/decide/assina. Explicação (grupo + técnica) entregue pro Eduardo. Candidato de copy parqueado: "Iniciar sua Entrevista".

## ⏱️ Timeline leve (últimos diários)
- **30/05** (2 partes): auditoria 360 + Sprint Tiers executado + societário (acordo quotistas v2.1 rascunho)
- **31/05**: UX/visual densa V1.9.534-544 + bug login Flávia (tokens NULL) + audit empírico (5 números recalibrados)
- **01/06**: 2 auditorias (coerência + Claudio) + cron forense (disparou 00:00 BRT, falhou null user_id, **0 poluição**) + landing V1.9.547-557 + conceito AEC + handoff laptop
- **02/06 manhã (laptop)**: V1.9.558-560 documents Curadoria/Meus + RLS tighten + pattern PowerShell UTF-8 (BLOCO A-J acima)
- **02/06 tarde/noite (desktop)**: doc FHIR R4 auditor-safe + slides Donizete + roadmap 5 frentes + landing V1.9.561 hero rede neural FECHADA/commitada `118804f` (PARTE 2 + BLOCO P)

> 📌 **Lembrete — Retrospectiva mensal existe**: `RETROSPECTIVA_MENSAL_2026_04_26_a_2026_05_25.md`. Consultar pra ancorar contexto histórico (30 dias) antes de feature nova.

## 🚀 Frase âncora 02/06 (desktop)
> *"Sessão regulatória: o doc FHIR saiu de 'pitch otimista' pra 'auditor-safe' via 2 workflows empíricos (PAT+código) + 2 reviews externos — pegando 2 overclaims (ICP no relatório / não-SaMD) que teriam queimado o filme com o Donizete (regulador veterano cético). Todos os números re-confirmados ao vivo (RLS 145/145, 150 reports). Próximo: trilha técnica (serializer FHIR #1); trilha humana (Marco 1 CNPJ) destrava o resto. Landing visual em preview aguardando decisão commit/revert. Locks 8 INTOCADOS."*

---

## BLOCO P (update ~noite) — 🎨 Landing V1.9.561 FECHADA + commitada (`118804f`, push 4 refs)
Sessão visual longa (iteração ao vivo via **loop Playwright screenshot** — `_shot.mjs` → `page.screenshot` → Read PNG; encerrou ~8 rodadas de chute às cegas). Hero **100% em código** (zero imagem raster nova):
- **Background neural SVG animado**: nós pulsando (framer-motion `r`+opacity) + sinapses (linhas gradiente) + ondinhas (anéis expandindo por nó). `userSpaceOnUse`.
- **Cerco simétrico**: rede direita (cyan→verde) abraça o emblema + **espelho esquerdo** (cyan→azul) via `scale(-1)` + **máscara-buraco radial** que "vaza" o centro pra rede não cobrir a logo. Caudas off-grid nas laterais + extensões bezier subindo no C esquerdo.
- **Emblema neuro-orgânica refinada** (~20% menor, contraste, glow suave). **Revertido experimento banner Gptest** (GPT recomendou 80% a emblema). Paleta verde→**teal**, fundo verde-amarelado leve, botão **ghost** "Criar Conta".
- Cleanup: dead code `lnodes`/`llines` removido, `_shot.mjs`/`gptest-logo.png` NÃO commitados. Type-check verde. Locks 8 INTOCADOS.
- Memória de processo: [[feedback_landing_visual_iteracao_screenshot_loop_02_06]].
- **Micro-ajuste pós-commit (uncommitted)**: bg mid `#1e293b`→`#1a3a47` (mais ciano, "acordada" leve) — pendente commit V1.9.562.

**Próximo (combinado): atacar as 5 frentes técnicas regulatórias — #1 Serializer FHIR PoC (risco zero, read-only).**

---

## BLOCO Q (noite) — ✅ FRENTE #1 EXECUTADA: Serializer FHIR PoC (V1.9.563 + doc `651b969`)
Atacada a 1ª das 5 frentes técnicas regulatórias. **Read-only, aditivo, zero lock tocado.**
- `src/lib/fhir/fhirR4.types.ts` (tipos R4 mínimos) + `src/lib/fhir/clinicalReportToFhir.ts` (função pura `clinicalReportToFhirBundle`).
- Bundle type=document: Composition (raiz) + Patient + Practitioner + ClinicalImpression[] (1 por racionalidade) + Consent + Provenance. `content.*` → `Composition.section[]`; rationalities (object por tipo) → ClinicalImpression; consent_given → Consent; **signature_hash → Provenance/extensão de INTEGRIDADE (SHA-256), NUNCA FHIR Signature** (overclaim guard cravado em teste).
- **9 testes unit** (fixture sintético, sem PII) + **validado ad-hoc contra report REAL via PAT**: 7 recursos, 10 seções, refs 6/6 resolvem, hash integridade ✅, Signature ausente ✅. Temp test + JSON real deletados (PII fora do repo).
- Doc INTEROP atualizado (§1/§6/§8/§9): "serializer inexistente / não demonstrada" → "PoC implementado + validado", honesto sobre o que falta.
- **Resta na #1**: endpoint HTTP `/fhir` (Edge ou rota) + validação contra validador FHIR oficial / perfis br-core.

**Próximas frentes (ROI×risco)**: #2 gaps SGQ (baixo) · #3 ICP no relatório (🔴 lock V1.9.299, sessão dedicada) · #4 terminologia CID-10/LOINC (alto, precisa Ricardo) · #5 br-core (depende do #1).

---

## BLOCO R (noite) — ⚖️ GAP-ANALYSIS REGULATÓRIA COMPLETA (workflow 19 agentes + PAT auto-verificado · commit `20cb3e9`)
Pedro pediu o mapa "100% aterrado, sem alucinar" do que falta pra estar dentro das normas. Rodado workflow multi-agente (10 web por norma + 7 PAT/código + síntese + crítico adversarial; 1.48M tokens) → **`docs/CONFORMIDADE_REGULATORIA_GAP_ANALYSIS.md`**. **Re-verifiquei os números EU MESMO via PAT (HTTP 201)** — o crítico flaggou PATs de memória em 401, então não confiei cego.
- **🔑 Correção de fato:** **CFM 2.454/2026 EXISTE** (DOU 27/02/2026, vigência ~26/08/2026) — refuta o "sem lastro" que estava no CLAUDE.md + memória interop. Artigos a confirmar no PDF oficial.
- **🚨 P0 atacável já:** leak PII (`sanitizeRationalityPII` regex token-exato não pega variantes/typos → **20/141 racionalidades não-pseudonimizadas**); RSK-001 H8 stale (verify_jwt já corrigido V1.9.506); RPC `calculate_ckd_stage` INSERT em `exam_type` (coluna real = `test_type`); cibersegurança rebaixada atende→parcial (evidência≠dossiê).
- **📊 Auto-verificado (docs estavam stale):** RLS **145/145**+467 (era 144/447), clinical_reports 150/47/101, cfm 52, exames 25 (signed_at NULL 25/25), pki 12 (signer_cpf placeholder 12/12), patient_conditions 0.
- **Novidade contexto:** RDC 1.015/2026 + **SNCR** (cannabis) = canal mais específico; 52 prescrições hoje texto livre.
- **Veredito:** PARCIAL em tudo (nenhuma 0, nenhuma 100%). Gargalo-mãe = **Marco 1 CNPJ** (~50% bloqueado). Trilha técnica anda já (6 ações sem CNPJ). Anti-overclaim preservado (crítico confirmou os 5 não-violados).
- Memória: [[project_gap_analysis_conformidade_regulatoria_02_06]].

**Próximo natural:** atacar os P0 técnicos (leak PII #1 → RSK-001 → RPC CKD → relatório IA → dossiê ciber) que não dependem do Marco 1.
