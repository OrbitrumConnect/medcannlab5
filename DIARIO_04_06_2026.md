# DIÁRIO 04/06/2026 — Análise do Core + fixes de consistência + remoção de dead code (madrugada)

> Continuação da sessão 03/06 (que já tem diário próprio). Foco: bugs empíricos achados via uso real (Gisele, Pedro Paciente) → análise da lógica inteira do Core → plano de redução de fragilidades sem regressão.

## Sequência da madrugada (HEAD `7b4cbb8`)

| V | O quê | Commit |
|---|---|---|
| **V1.9.580** | Guard anti-leak RAG durante AEC ativa sem hint (bug Gisele 03/06) | `5c4accc` (Core v426) |
| **V1.9.581** | RPC canônica `get_user_stats` — fim da divergência Nôa/UI (3 fontes com caps diferentes) | `e313bef` + `a792267` |
| **V1.9.582** | **Remove handler finalize v2 morto** (inalcançável desde Gate D') | `7b4cbb8` (Core v427) |

## BLOCO A — Bug Gisele (V1.9.580): RAG sequestrou a escuta clínica
AEC ativa (COMPLAINT_LIST), paciente relatou CBD perfeitamente, mas a resposta derrapou pra "documentos/base de conhecimento". metadata: `tokens=0` → **não foi GPT, foi template determinístico no branch errado** (RAG do CBD sequestrou o roteamento). Família V1.9.318. Fix: guard que durante AEC ativa devolve escuta neutra, nunca cai no builder de chat livre.

## BLOCO B — Divergência de stats (V1.9.581): 3 fontes descoordenadas
Nôa dizia "20 avaliações/125 dias", UI "45 AECs/104 dias". Raiz: cada tela contava com seu `.limit`:
- buildPatientContext (Nôa): `clinical_reports.limit(20)+length` → capava 20
- PatientsManagement (prontuário): `.limit(10)+length` → capava 10
- PatientStats (widget): "dias desde o PRIMEIRO relatório" (104) em vez de criação da conta (125)
Fix: **RPC única `get_user_stats`** (dias=`public.users.created_at`, avaliacoes=`count(clinical_reports não-draft)`). Os 3 consumidores ligados nela. Smoke universal: 5 pacientes, RPC=real 100%.

## BLOCO C — Análise da lógica INTEIRA do Core (mapa de referência)
O Core é **um único `Deno.serve` de ~6000 linhas** onde a "pirâmide de 8 camadas" é a **ordem física dos early-returns**. Defesa em profundidade real e empiricamente endurecida (identidade só-JWT, Gate D' fail-closed duplo, Verbatim First, RAG silenciado em AEC). Descoberta central: **o Core CONFIA no estado que o frontend computa** (FSM/hints/userContext via 9 arquivos) — muitos "bugs do Core" nascem nos feeders do frontend (o `.limit(20)`, o RAG sem hint).

**Fragilidades (estruturais, não de segurança):** prompts gigantes inline (~1000 linhas), governança por ordem de código, guards de agendamento regex-frágeis (histórico de regressão), role de `profiles.type` vs `users` canônica, motor determinístico ainda vaza RAG fora da AEC, catch global 200 mascara incidentes, pipeline 3-4 OpenAI serial.

## BLOCO D — V1.9.582 remoção do dead code (protocolo completo)
Handler `if(action==='finalize_assessment')` (era 3585-3864), "GOLDEN 03/04". **Estruturalmente inalcançável desde o Gate D' (V1.9.100-P0b, 29/04):** `action='finalize_assessment'` é interceptado e RETORNA na ~linha 2573 (caminho vivo gated) antes de chegar lá. Confirmado por **4 ângulos**: back (return da 2573), front (clinicalAssessmentFlow.ts:1920 → caminho vivo), doc (`P0B_GATE_D_DRAFT.md` já catalogava como "inalcançável dead code"), e o próprio TODO no código ("validar via logs → se nunca, DROP").

**Risco fechado:** o bloco morto tinha fallback hardcoded do Ricardo e **NÃO passava pelo Gate D'** — se ficasse alcançável, finalizaria AEC sem checar médico vinculado.

**Protocolo executado:**
1. ✅ Commit isolado (`7b4cbb8`) — diff = deleção pura (300 del / 2 ins = breadcrumb + newline EOF); código vivo intocado (handleFinalizeAssessment 1223, isFinalizeRequest 2425/2429, PREDICT SCHEDULING preservado)
2. ✅ deno check: 5 erros pré-existentes, **0 novos**
3. ⚠️ Testes `aec-finalize-schema.test.ts` + `consent-gate.test.ts` existem mas exigem `SUPABASE_SERVICE_ROLE_KEY` + batem em PRODUÇÃO → NÃO rodados (risco de dados); o caminho que testam está byte-idêntico
4. ✅ Deploy produção **v427** + smoke 401/401 + verify_jwt true
5. ✅ Este registro

## BLOCO E — Plano de refator do Core (convergência com GPT externo)
Auditoria externa cruzada (GPT, sem código/PAT) **convergiu** com a análise empírica (com código/PAT) — sinal de diagnóstico robusto. Plano em 3 fases, **sem tocar comportamento clínico**:
1. **Reduzir ambiguidade (quick wins):** ✅ dead code (V1.9.582) · const RICARDO_UUID + helper isRationalityMessage · guard RAG fora da AEC · logar erros do catch em system_health_alerts
2. **Modularizar (com rede de teste ANTES):** testes de fixtures pros guards de agendamento → `intentEngine.ts` (centralizar) · extrair prompts (`.ts` template, têm interpolação) · role `profiles`→`users` com smoke matrix
3. **Estrutural (sessão dedicada):** mapa de roteamento explícito (ROUTES) preservando ordem exata · paralelizar pipeline · observabilidade surface (a telemetria já existe em noa_logs/metadata.model, falta dashboard)

**Princípio:** reduzir ambiguidade → modularizar → repensar arquitetura. Cada passo: slug-test/deno check + smoke + locks intocados.

## BLOCO F — 🏗️ Migração de Base Clínica: motor + executor + arquitetura de execução (V1.9.583-586)

Pedro decidiu "montar o app, jurídico revisa depois" (modelo ajustável via terms_version; mais barato refinar texto que refazer arquitetura). Construído incremental, cada peça testada, **sem regressão**:

- **V1.9.583** — 6 colunas de termo em import_batches (terms_accepted/at/version/hash + accepted_ip/user_agent) + texto do **Termo de Responsabilidade pela Importação** (rascunho V1 + 2 refinamentos: direitos titular LGPD Art.18 + dever de informação) na spec + `buildImportPlan` (dry-run puro).
- **V1.9.584** — `executeImportPlan` (real load como LÓGICA: dedup CPF→email + resolve Id-EMR→UUID + proveniência + órfãos), testado com **mock ImportDb** (3 testes). 25 testes no motor total.
- **V1.9.585** — adapter `emrSupabaseDb.ts` + 🚨 **ACHADO EMPÍRICO via smoke (PAT):** `patient_medical_records.patient_id` e `patient_documents.patient_id` têm **FK→`auth.users`** (não public.users). Pego ANTES de construir errado. Implicação: paciente importado precisa de identidade auth → criar via `auth.admin.createUser` (sem credenciais = sem blast) → **real load roda numa EDGE** (service role). Decisão de destino cravada: histórico → patient_medical_records (record_type prescription_history/exam_history), NÃO polui cfm_prescriptions/patient_exam_requests (assinados ICP).
- **V1.9.586** — Fase 0 (trigger `handle_new_user` validado: cria public.users de metadata.type) + Fase 1 (colunas progresso: processed_count/error_count/error_log/started_at/finished_at).

**Convergência GPT (auditoria externa, sem código):** confirmou o achado + cravou 5 requisitos da Edge (chunks assíncronos / idempotência filhos namespaced / falha parcial / observabilidade / placeholders) + elogiou a separação importado-vs-gerado como "decisão mais importante da arquitetura". Convergência com a análise empírica = confiança alta.

## BLOCO G — 🛑 Por que parei aqui (decisão de checkpoint)

Recomendei **fechar o marco arquitetural** e deixar a Edge (Fase 2) pra sessão fresca. Razões:

1. **Modelo empírico = investigar com calma > construir com pressa.** Os 3 maiores achados do dia (FK auth.users, .limit(20), dead code inalcançável) vieram de **investigar antes**, não de codar rápido. A Edge merece a mesma calma.
2. **A Edge é a peça MAIS sensível:** cria **identidades auth REAIS em produção** (service role). Pressa aqui é o oposto da disciplina que nos manteve sem regressão a sessão inteira.
3. **Edge assíncrona ≠ trivial:** chunks + cursor + falha parcial + idempotência namespaced + slug-test + piloto pequeno. É um **build novo de Deno**, não um ajuste — sai melhor com fôlego.
4. **Checkpoint limpo > peça meio-construída.** O marco arquitetural está sólido, testado, committed, com os requisitos da Fase 2 na memória. Uma Edge pela metade não seria um bom ponto de retomada.
5. **Maratona longa:** a sessão acumulou landing + cascade regulatória + Core (análise/limpeza) + stats + import inteiro. O melhor a fazer agora é **cristalizar**, não empilhar a peça crítica no cansaço.

**Princípio:** parar é decisão de engenharia, não de desistência — o marco arquitetural está fechado e a próxima sessão começa exatamente onde paramos, com os requisitos cravados.

## 🚀 Frase âncora 04/06
> *"Os bugs da madrugada (RAG vazando, .limit(20), handler morto) NÃO são falhas de produto — são sintomas de um Core que concentrou responsabilidade demais num arquivo gigante, onde a segurança depende da ordem física do código. A lógica clínica está protegida por camadas; a fragilidade é o conhecimento implícito ('esse bloco vem antes daquele'). A análise empírica (código+PAT) e a externa (GPT, só diários) convergiram no mesmo diagnóstico — confiança alta. Plano: reduzir ambiguidade → modularizar → repensar, sempre sem tocar o comportamento clínico que funciona. Dead code removido com prova estrutural de 4 ângulos. Locks 8 intocados."*
